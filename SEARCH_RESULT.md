# ganasadai1 探索修正版 実測結果
## 修正内容
前回版の「終了MPを初期MPと同じ80以上にする」という独自条件、途中HP・MP残量への加点・減点、途中HP200判定によるバケット分類を撤回した。途中HPが1以上なら、HPの少なさでは順位を下げない。途中MPが終了条件未満でも、そのことだけで経路を禁止しない。戦闘処理・乱数・敵AI・カメラ・装備値は変更していない。
## HP・MP条件の出典
初期MPは `main.cpp` の `ganasadai` 用 `copiedPlayers[0].mp = 80` を変更せず使用する。入力の現在世界も書き換えない。
勝利時HP200以上は旧 `ActionOptimizer.cpp` の `RequiredAllyHpOnVictory = 200` を維持した。敵HPが0になった時点と、元の世界からのfresh exact replay結果で判定する。撃破後に回復コマンドを追加することは認めない。
**旧コードに独立した数値の「終了MP定数」はない。** 旧 `ACTION_TABLE` の攻撃候補 `MULTITHRUST` は使用前 `mp >= 10`、`BattleEmulator.cpp` の実消費は4である。この条件から得られる撃破後の残量6を、`RequiredVictoryMp = 10 - 4` として終端に維持した。新たに許可された最終ターン限定の通常攻撃でも、この残量を下回る勝利は採用しない。これは「旧コードに直接 `finalMP >= 6` という行がある」という意味ではなく、元の候補条件と実消費から導出した下限である。前回の `finalMP >= initialMP` は根拠がないため採用していない。
途中のMPチェックは、行動を実際に実行できる消費量だけで行う。途中のさみだれづきはMP4以上、ベホマはMP24以上で使える。途中MPが6未満になったこと、または聖水を使い切ったことだけでは枝を落とさない。敵撃破時にHP・MP条件を満たせない候補は不採用とする。
採用結果は以下の全実測で終了MP25であり、旧デフォルトの採用結果と同じ残量だった。
## 現DEBUG3デフォルトの比較
入力は seed `0x450ff41f`、固定prefix `[30, 53]`、初期MP80。引数なしの既存 `DEBUG3 -> SearchRequest` を使用した。比較元は最初の添付の旧探索本体で、最新添付と戦闘本体・Player・装備・乱数・カメラの内容が同じことを確認した。
|実装・候補|ターン|exact position|exact装備変更回数|終了HP|終了MP|経路中の最低HP|
|---|---:|---:|---:|---:|---:|---:|
|旧SearchRequestが選択したTableB|10|23|6|234|25|2|
|旧TableDの候補|10|23|4|216|25|7|
|修正版・採用variant 12|10|23|4|216|25|7|
修正版は旧版の最良候補と同等であり、旧TableDを超える新しい勝利列が見つかったという報告ではない。旧SearchRequestの実際の選択結果に対しては装備変更が6回から4回に減り、前回提出した13ターンへの悪化を解消した。今回の比較で9ターン以下の条件付き勝利は見つからなかった。
勝利候補は探索から毎回生成され、コードにseedや既知行動列を埋め込んでいない。採用経路ではHP7まで低下してから8ターン目にベホマを使用し、最終HP216で撃破する。詳細は同梱ログにある。
## 1500ms予算
採用版の引数なし実行を3回行い、すべて上表と同じexact結果を得た。
|実行|探索時間ms|実際の1ターン遷移呼び出し回数|fresh replay|
|---|---:|---:|---|
|1|1496.70|7,681,408|一致|
|2|1497.01|7,850,496|一致|
|3|1496.31|8,572,928|一致|
探索回数は `expanded` の値であり、重複除去前の候補や先読みも含む実エミュレータの1ターン遷移回数である。ユニークstate数ではない。固定ノード数や小さい幅で打ち切らず、共通deadlineまで探索する。総予算にはprefixの実行と採用候補のfresh replayも含む。
## 採用方式と比較
採用variantは **12: burst-schedule-and-beam**。前半ではテンション・すてみ・回復等の順序と装備変更を実エミュレータで列挙し、後半ではHP・MPを評価値に含めないビーム探索を行う。前半は総予算の約40%、残りを後半に使用し、別々に1500msを割り当てない。戦術列挙は現在状態から行う戦術クラスの探索で、特定seedの答えではない。一般ビームには途中のさみだれづき連打も残している。
途中順位は敵HP、テンションによる攻撃準備、攻撃・防御強化、結果position等で作る。HP・MPは加点・減点に使わず、バケットも攻撃準備・装備・防御強化で分類する。同一stateの照合には、正確さのためHP・MPを含む全Playerフィールドを使う。
広幅・攻撃準備重視・直接ダメージ重視・position重視・多様化・先読み・局所修復・装備固定・ミラーシールド許可等を比較した。代表的な最終条件下の実測は以下のとおり。
|variant|内容|ターン|position|装備変更|探索時間ms|
|---:|---|---:|---:|---:|---:|
|0|複数優先順位のビーム|10|23|4|1495.52|
|4|直接ダメージ重視|11|26|2|1498.36|
|5|ミラーシールド許可との比較|10|23|4|1498.42|
|6|装備固定との比較|11|26|0|1496.37|
|9|実エミュレータ先読み|10|23|4|1496.86|
|10|position・多様化|10|23|4|1498.44|
|11|position重視|10|23|4|1498.28|
|12|採用版|10|23|4|1496.70|
装備変更を外すと11ターン・position26へ悪化した。追加比較でも採用版より小さいpositionや同positionで少ない装備変更は得られなかったため、ここで比較を終えた。最適性の証明は行っていない。
## 近傍seedでの採用版
固定prefixは同じ `[30, 53]`。すべて敵HP0、HP200以上、MP6以上、fresh exact replay一致。
|seed|ターン|position|装備変更|終了HP|終了MP|最低経路HP|探索時間ms|
|---|---:|---:|---:|---:|---:|---:|---:|
|0x450ff420|10|23|0|259|25|53|1495.90|
|0x450ff447|10|23|2|266|25|2|1496.68|
## 制約と接続
さみだれづきは、そのターンの装備選択後に武器を装備している場合のみ使用する。通常攻撃はそのターンで敵を倒せる場合だけ採用する。持ち越し休み・睡眠・麻痺で装備変更できない状態では変更しない。不正なFLEEスキップは使わない。採用variantはミラーシールドを生成しないが、戦闘側の処理や敵AIは削除していない。
`SearchRequest -> GanasadaiSearch::Run`、`DEBUG3 -> SearchRequest`、`ActionOptimizer::RunAlgorithm -> GanasadaiSearch::Run` の既存接続を維持し、標準variantを12にした。WebAssemblyは `wasm_search_dump -> buildDumpOutput -> SearchRequest` で同じ探索を使用する。`webassembly/build.sh` の `ganasadai1_gouketu`、`-Dganasadai=1`、`GanasadaiSearch.cpp` の組み込みを維持した。
通常ReleaseビルドとDEBUG3ビルドは成功。実際のコンパイルはC++17・O3で行った。既存ヘッダーの最適化表示はCMakeのマクロ判定に依存するため、実効フラグは同梱の `benchmark/debug3_compile_flags.txt` を参照する。WebAssemblyビルドスクリプトはCRLFをLFへ正規化して `bash -n` を通した。環境に `emcc` がないため、WASM実ビルドとブラウザ実行は未確認であり、成功とは報告しない。
## 再現コマンド
```bash
cmake -S . -B build-debug3 -DCMAKE_BUILD_TYPE=Release -DCMAKE_CXX_FLAGS="-DDEBUG3=1"
cmake --build build-debug3 --target ganasadai_gouketu -j 4
./build-debug3/ganasadai_gouketu
```
通常Releaseは `-DCMAKE_CXX_FLAGS="-DDEBUG3=1"` を付けず、別ビルドディレクトリで構成する。比較variantを実行する場合は、既存DEBUG3の `--search-variant` と `--search-budget-ms` を使用する。既定入力は変更していない。