**最短ターン数の証明仕様 — 近似による d(N−1)=偽 と、同じ規則で再実行する d(N)=真**

2026-09-05。`aaa.txt` にあるGPT5.6 solの指摘を受けた設計補足。
実装は行わない。本書は `d20-worldline-proof-spec.md` の第5・10・14・15・17節を具体化する。

同日追記。現行ソースを調査し、Invariantの責任と領域の形式を第2・4・13〜16節で固定した。
初版のInvariantを任意の関数として実装してはならない。初版ではkernelが作る固定形式の外包だけを使う。
目標は、同じ入力・規則について勝利手順による `d(N)=真` と、外側近似による `d(N-1)=偽` を組にすること。
数百ms〜15秒は生成・検査・再実行を含む実測の目標であり、本書には特定入力の証明書や達成時間はまだない。
以下のv1の制限は、先行する本体仕様の一般的な記述より優先する。

最大の難所は、DPへ渡す一ターンの遷移を、正しい領域と更新式に分解することにある。
次に難しいのが、それを粗く近似した際の架空の接続を、少数の分割で除けるかである。
DPの辺評価回数だけを根拠に、全体が数百ms〜15秒に収まるとは見積もれない。

特に、検査器が生成済みの辺だけを検査しても、生成器が落とした枝を検出できない。
以下では、**検査器が次に調べる命令と入力領域を自分で決める**方式にする。

**1. 信用する部分を固定する**

証明生成器から渡された命令列を、無条件に戦闘の定義として受け取ってはいけない。
検査器側に、対象戦闘の一ターンの規則を固定した `RuleProgram` を置く。
各命令は固定された位置 `pc` を持ち、分岐先、呼び出す関数、更新先もこの規則で決まる。

信用する基盤は次の三つ。

1. 指定された戦闘規則に対応する `RuleProgram`。
2. 資源に依存しない乱数・ダメージ計算の限定された既存関数。
3. 整数式、区間、命令順序、証明記録を検査する小さい検査器。

探索・価格の選択・セル分割の選択・証明生成器・生成済みの辺一覧は、この信用の基盤に含めない。

ただし、`RuleProgram` が現C++と利用者の指定した修正規則に対応することは、別の検証対象である。
行番号や関数名を書いただけでは、その対応は証明されない。
元の比較・更新箇所との対応表を作り、分岐順序も含めて監査する必要がある。
本案はC++全体の意味を自動証明するコンパイラを要求しない。
その代わり、この対応を未検証のまま「現BattleEmulator全体まで形式検証済み」とは呼ばない。

対象モデルには、少なくとも以下の規則の選択を明記する。

```text
HEALの選択条件：MP>=2
CRACKの選択条件：MP>=3
眠り・麻痺中のFLEE禁止、および行動実行時の再判定
敵先攻時の状態異常と、その後の味方行動
カウンターによる敵HP更新と、その後の味方行動
v1では初期状態が眠っておらず、対象行動が眠りを書き込まないこと
```

FLEEの旧skipTurnを、そのままこの規則として取り込まない。
未定義の眠りの動作も推測で埋めない。未対応なら、検証済みの上限で覆うか未判定とする。
v1では眠りを含む入力を未対応とする。眠りなしを保証できる対象は第13節に限定する。

**2. 記号実行器が扱う型を小さく限定する**

四つの入力資源を `e,a,m,i` とする。
途中の資源式は、**各資源自身の入力座標**について次のどちらかだけを扱う。

```text
E_expr = e + k / k
A_expr = a + k / k
M_expr = m + k / k
I_expr = i + k / k
k: 検査された符号付き64bit整数。上の / は選択肢の区切り。
```

`E_expr=a+k`、`E_expr=2*e+k`、座標間の大小比較、任意の述語コールバックは受理しない。
固定ダメージを引くこと、固定回復量を足すこと、clampの後に定数にすることを扱う。
ダメージ自体は、必要な制御条件を分割してから既存の限定関数で具体的に計算する。

一個の領域の形式を次に固定する。

```text
Box = [le,ue] × [la,ua] × [lm,um] × [li,ui]
      × mask(Charge) × mask(Paralysis) × mask(Acro)
      × mask(Rage) × mask(Inactive) × mask(Camera)

資源区間は両端を含む整数区間。
modeの値とbit番号は第13節の固定enum。未定義bitは拒否する。
p、seed、選択コマンド、経過ターンtはBoxの外の具体値。
```

制御式は `Const(value)` または `Map(j, table)` のみ。
後者は同じ制御座標jの入力enum値を、小さい定数表で途中値へ写すものとする。
表の入力は最大8値なので、合成・像・逆像はその8値以下を調べればよい。
別座標に依存する更新は、その条件で先に分割してから定数代入等を行う。
途中の麻痺タイマー-3等は規則のpcで定めた一時値として扱い、境界enumには追加しない。
配列参照やnative関数の引数が未確定なら、その使用箇所で必要な値に分割する。
制御状態全体の直積列挙は行わない。

セル分割木で認める原子条件も、次の二つだけとする。

```text
入力資源X <= 整数k
入力制御j ∈ 固定enumのbitmask
```

規則の比較は現在の式を代入してこの形に戻す。
`X<k` は `X<=k-1`、等値は二つの閾値分岐で表す。境界計算もオーバーフローを検査する。
`2*E<456` のような固定正整数倍の比較は、対応を監査した整数閾値へ変換する。
論理積・論理和は規則の短絡評価順序に従う複数の分岐pcにし、任意式ソルバーを呼ばない。

kernelに必要な領域操作は以下で尽きる。

| 操作 | 実装する検査 |
| --- | --- |
| 空判定 | いずれかの区間で下端>上端、またはいずれかのmaskが0 |
| 共通部分 | 区間は下端のmax・上端のmin、maskはAND |
| 包含 | 全区間の端点包含、全maskで `source & ~target == 0` |
| 閾値の真偽分割 | 該当する一区間だけを `<=k` と `>=k+1` に制限 |
| modeの真偽分割 | 対象maskとのANDと、その型の範囲内での補集合とのAND |
| 更新後の像 | 自座標の平行移動・定数、または小さい写像表の像 |
| 出力セルの逆像 | 出力の分割木をたどり、資源式への代入または写像表の逆像で入力を制限 |

各セルはBaseBoxを上の原子条件で分割した木の一葉とする。葉は一個のBoxになる。
不連続な集合は複数の葉・証明枝で表す。任意のBox和集合、DNF、多面体、DBM、SMTはv1に入れない。
凸包で二つの枝を通常葉へ合併することもしない。合併するなら、全体を覆う補完として検査する。
葉数だけでなく、これらの領域操作・写像表の処理・出力木の走査も第10節のVに数える。

この形式では資源式が自座標だけに依存するため、詳細葉の資源の像も直積のままである。
重みの最大値は `e=ue, a=la, m=lm, i=li` で計算できる。
これは式の係数の符号から得られる厳密な最大値であり、代表状態による試行ではない。

検査中の環境は概念的に

```text
Frame = (pc, call_stack, RNG_position, selected_command,
         control_expressions, E_expr, A_expr, M_expr, I_expr,
         action_progress, input_domain)
```

である。`action_progress` は、味方・敵の行動が未実行か実行済みか、
資源消費まで進んだか等を規則上の実行位置から決める情報である。
生成器が「未実行」と自由に宣言する情報ではない。

実状態の保存は引き続き可逆な `uint64_t[3]` を使える。
`Frame` は局所証明を読むための作業領域であり、到達した全個体の保存ではない。
この作業領域や証明記録の容量は、実状態24byteとは別に計上する。

**3. 検査可能な命令と証明記録**

最小構成は次の種類で足りる。

| 規則側の命令 | 検査器が行うこと |
| --- | --- |
| 定数代入・加減算 | 現在の式から更新式を再計算。オーバーフローを検査 |
| 資源または制御の比較 | 真・偽の入力領域を再計算し、必要な両方の子へ進む |
| 固定有限集合のswitch | 全該当値の領域と分岐先を再計算。省略された値を検出 |
| 乱数読み出し | seedと現在位置から値を再計算し、演算形式と位置の進行を確認 |
| 固定個数の乱数読み飛ばし | 規則に書かれた個数だけ位置を進める |
| 限定された計算関数 | 規則側が指定する関数を、環境から決まる引数で再実行 |
| ジャンプ・呼び出し・復帰 | 規則側の次pcとスタックを確認 |
| 正常終了・成功・失敗 | 到達すべき終了位置、生死、出力式を確認 |
| 検証済み上限による中断 | その実行位置で使える補題と、その適用条件を確認 |

既存関数を一個の命令として扱えるのは、渡す引数が具体的に決まり、
隠れた資源読み出し・書き込みがない場合に限る。
例えば固定atk・defと乱数位置からダメージを計算する関数では、値と消費後位置を再計算できる。
`callAttackFun` 全体、`ProcessRage`、`process7A8` を資源に無関係な黒箱として扱ってはならない。
その中にある資源比較、状態変更、追加乱数消費を命令列に含める必要がある。

小さい固定回数のループや一段のカウンター呼び出しは、その規則上の順序で処理する。
一般的なSMT処理系や、任意のC++を解析する仕組みは要求しない。

証明記録のノードは、例えば次を持つ。

```text
node_id
expected_pc
kind = STEP / SPLIT / SWITCH / FINISH / COMPLETE
claim_payload
child_ids
```

入力領域と途中の式は、原則として親から検査器が計算する。
ノードにそれらの値を保存する場合も、計算した値と完全一致するか確認する。
ノードごとに全状態を重複保存する必要はない。

**4. 根と子を、生成器に決めさせない**

検査器は各必要な `(経過ターンt, 乱数位置p, セルq)` と8行動について、自分で次を計算する。

\[
D_{t,q,b}=\operatorname{Envelope}_t\cap\operatorname{Cell}(q)
\cap\operatorname{Selectable}(b).
\]

根領域が空でなければ、その行動の証明木が必須である。
証明書に行動の根が載っていないことを、「その行動は不可能」と解釈しない。
根領域が空という主張も、検査器自身が確認する。

旧記法の `Invariant` は第14節の `Envelope_t` に置き換える。
これは入力と固定enumからkernelが組み立てるBoxとpの範囲であり、生成器が供給する関数ではない。
`Selectable` も固定する。HEALは `M>=2`、CRACKは `M>=3`、薬草は `I>=1`、
ACROは `Charge=ON(1..6)` かつ `Acro=OFF`、FLEEは `Paralysis=CLEAR`。
眠りなしは入力プロファイルが保証する。ATTACK・DRAGON・DEFENCEには追加の資源条件はない。
行動実行時の状態異常による差し替えは、根を消す条件ではなく規則内部の遷移である。

未到達セルを省略する場合は、被覆済みの辺から再計算した支持集合などの根拠が必要になる。
セルや行き先が表に存在しないというだけでは省略できない。
第14節に、この支持集合をkernelが前向きに作る手順を定める。

比較 `g` に出会ったら、検査器が

\[
D_T=D\cap g,\qquad D_F=D\cap\neg g
\]

を計算する。空でない側の子が欠けていたら不合格。
両方が非空なら、両方を検査しなければ根の検査は完了しない。

たとえば、この箇所の確定ダメージが16、入力敵HPが `[114,456]` なら、

```text
比較：E-16 < 228
真側：入力E ∈ [114,243]
偽側：入力E ∈ [244,456]
```

となる。これは説明用の局所例であり、特定seedの実行結果を報告したものではない。
偽側を省略した証明は拒否する。
真側を `[114,242]` にしてHP243を欠落させた証明も拒否する。

ただし、両側の和集合が親と一致するだけでは足りない。
**比較式そのものが、現在pcで規則が要求する比較でなければならない。**
生成器が本来の怒り判定を飛ばして、無関係な比較で領域を覆っても拒否する。

同様に、乱数記録に `p=123` と書かれていても、それを次の位置として信用しない。
現在の環境から得た位置が123なのかを確認し、そのseedの値を読み直す。
途中の読み飛ばし一個を欠落させれば、以降のpcまたは乱数位置で不一致になる。

**5. 検査手続き**

以下のtとEnvelopeは、第14節の根検査が渡す検査コンテキストから取得する。
証明ノードに書かれたターン数や外包へ置き換えない。

```text
verify_node(program, expected_frame, proof_node):
    assert proof_node.expected_pc == expected_frame.pc

    if proof_node.kind == COMPLETE:
        assert completion_lemma_is_allowed_here(expected_frame)
        summary = verify_completion_lemma(expected_frame, proof_node)
        assert summary has a checked output envelope, including terminal resources
        assert every continuing output lies in Envelope[t+1]
        emit_checked_summary(summary)
        return

    instruction = program[expected_frame.pc]

    if instruction is deterministic update / RNG / approved native computation:
        next_frame = independently_execute(instruction, expected_frame)
        assert payload agrees with the recomputed values
        verify_node(program, next_frame, required_child(proof_node))

    else if instruction is branch:
        for each branch required by instruction:
            child_domain = exact_restriction(expected_frame.domain, branch.condition)
            if child_domain is not empty:
                child_frame = advance_to_prescribed_branch(expected_frame, branch, child_domain)
                verify_node(program, child_frame, required_child(proof_node, branch))

    else if instruction is finish:
        assert claimed_resource_updates == expected_frame.resource_expressions
        assert claimed_terminal_kind agrees over the entire input domain
        outputs = exact_image(expected_frame)
        assert continuing outputs are contained in Envelope[t+1]
        assert terminal outputs have nonnegative HP, MP and item counts
        assert claimed_destination_contains_all_outputs(expected_frame)
        recompute_weight_bound_and_emit_checked_edge(expected_frame)

    else:
        reject
```

葉は、規則の終了位置へ到達した場合か、適用可能な補完補題を検査した場合だけ認める。
比較を処理する前に通常の終了葉を置くことはできない。

出力先セルの検査は、更新式に出力セルの述語を代入した逆像で行う。
一個のセルに入ると主張するなら、入力領域全体でその述語が成立する必要がある。
複数セルへ分かれるなら、入力領域をさらに分割するか、全出力先を含む補完として扱う。
包含検査の前に出力を `Envelope[t+1]` と交差させてはならない。
継続出力が外へ出るなら、その証明書を拒否する。
成功・失敗終端は継続用Envelopeとは別に分類し、終端資源の非負と終了位置を検査する。
補完の適用箇所と形式は第15節のホワイトリストに限定する。

実装を小さくするため、最初は木として検査してよい。
共有ノードを許す場合は、参照先のpc・環境・領域との適合も検査する。
同じノード番号というだけで別の途中状態へ証明を流用してはならない。

**6. この検査で、枝落ちを検出できる理由**

根の任意の具体的な入力状態を一つ選ぶ。
定数計算と乱数命令では、検査器が規則から結果を再計算するため、実行との対応が保たれる。
分岐命令では、その入力は検査器が作ったいずれかの非空領域に含まれ、その子は必ず存在する。
したがって、実行を進めても証明木から入力が消える箇所はない。

終了位置まで進めば、その入力の出力は検査済みの葉の式と出力先に含まれる。
途中で補完に進む場合も、検査済み補題がその入力からの残り実行を含む。
根がすべての合法入力を覆うので、各具体的遷移は検査済みの辺または補完に含まれる。

これで初めて、本体の第8節にある「全実遷移が被覆される」という仮定を満たせる。
検査済みの葉から辺を導出し、その辺についてDPの不等式を調べる。
生成器が別途提出した辺一覧だけを正本にしない。

この結論は、第1節で固定した規則と限定された計算関数に対するもの。
その規則自体の写し間違いまで、被覆木の検査だけで検出できるとは主張しない。

**7. セルを細分化しても、戦闘の記号実行を最初から繰り返さない**

計算を次の二つに分ける。

```text
規則の実行：ガード領域 D と、そこで成立する更新式 F を得る
セルへの接続：D と F を現在のセル分割に当てはめ、辺を得る
```

詳細な葉の記録は、固定seed・入力乱数位置・行動・規則・入力ガードに対応させる。
そこに価格や、現在のセル番号を埋め込まない。
セルへの接続と価格は、この記録から作る派生情報とする。

例えば元のセル `C` を `C1,C2` に分ける場合、既存葉 `(D,F)` は

\[
(D\cap C_1,F),\qquad(D\cap C_2,F)
\]

に制限できる。空の側だけ除く。
既に `D` 全体で更新式を検査しているので、その部分領域で乱数・ダメージを計算し直す必要はない。

出力先を `C1',C2'` に分ける場合も、

\[
D\cap F^{-1}(C'_j)
\]

で入力領域を分割する。
例えば `E'=E-16` と `E'<=227` なら、入力を `E<=243` で切るだけでよい。
clampや制御値の更新も、検査済みの式を使って逆像を計算する。

この再利用の根拠は単純である。
「Dの全点で成立する更新式」は「Dの部分集合の全点」でも成立する。
出力述語の逆像で切れば、各葉の出力先も正しく分類される。
この二つは新しい戦闘実行を必要としない。

補完葉には正確な残り更新式がないので、これを詳細葉と同じように使ってはいけない。
補完の対象領域を狭めることはできるが、詳細が必要になった部分だけは、
保存したpcと式から記号実行を再開する。
そこで得た詳細葉も、後の分割で再利用する。

「同じ部屋」「同じ乱数位置」だけを再利用条件にしてはならない。
規則、seed、開始pc、制御条件、資源式、証明された領域との包含が必要である。
保存する識別子は検査済み記録への添字でよく、実状態ハッシュ表は不要。

再利用のために古い証明やセルの写しを無制限に残すこともしない。
証明ノード・派生した辺・更新式の記録に、総byte数と総個数の上限を持たせる。
共有する記録と、現在の分割に依存する参照を分けて計上する。

検査済みのガードと資源増減ベクトルは、非負の価格を変えても利用できる。
この検査を価格候補ごとに繰り返す必要はない。
最終検査でも、検査器自身が同じ実行内で確定した記録を使える。
生成器が付けた「検査済み」フラグを信用することとは区別する。

**8. 補完辺に「何でもできる残り一ターン」を毎回与えない**

本体にある

\[
W_{\rm top}=QD_{\max}+uA_{\max}
\]

は安全な粗い上限だが、攻撃を選んだ辺にも無料の回復を許してしまう。
最大経路がこれを繰り返す場合、価格の調整だけでは改善しにくい。
実装では、行動と実行位置から直ちに分かる制約を補完にも残す。

以下は現在の8行動、固定能力値に対する**一ターン全体の上限**である。
通常攻撃の最大64、DRAGON・CRACKの直接ダメージは保守的に最大34、
一回のカウンターは最大64という上限を使う。
能力値や行動集合を変えた場合は再検証する。

| 選択した行動 | 使える補完重みの上限 |
| --- | --- |
| ATTACK | `128 Q` |
| DRAGON | `98 Q` |
| DEFENCE、FLEE、ACRO | `64 Q` |
| CRACK | `max(64 Q, 98 Q - 3 v)` |
| HEAL | `64 Q + max(0, u*gH - 2 v)` |
| 薬草 | `64 Q + max(0, u*gI - w)` |

`gH` は一回のHEALによる味方HP増加の上限で、粗く最大HP65を使える。
`gI` は薬草による増加の上限で、現在の計算では39以下。
敵の与ダメージは味方HPの増加を大きくしないので、これらは安全な上限である。
敵の吸収回復も敵HPの正味の減少を大きくしない。

回復しない行動には、味方HPの正の増分を与えない。
一方、FLEEの直接ダメージが0でも、敵行動に対するアクロのカウンターは起こりうる。
一ターン全体の敵HP変化を一律0とせず、それが不可能と分かった箇所だけで0へ締める。
FLEEの辺自体は、重み0になっても消さない。

CRACK・HEAL・薬草を二つの場合に分けた理由は、行動不能や戦闘終了によって、
選んだコマンドが実行されない場合を含めるためである。

```text
HEALが実行された：HP増加<=gH、MP増分=-2
HEALが実行されなかった：HP増加<=0、MP増分=0
```

この二つの組を保つので、HP増加だけ得てMP消費を免れる補完にはならない。
CRACKも、直接ダメージを得る側ではMP3を必ず消費する。
全場合へMP消費を押し付けるのも、全場合で消費を0にするのも避ける。

これらは個体の優先順位ではなく、全該当遷移が満たす資源収支の補題である。
検査器が行動、進行状況、固定能力値から適用可能性を確認する。
表の値を、証明生成器が自由に小さく変更することはできない。

**9. 記号実行の途中で止める場合は、実行済みの収支を保持する**

入力を `S`、中断位置まで進んだ状態を `Sc=Fprefix(S)` とする。
残りのプログラムに対して使える重みの上限を `Uremaining` とすると、補完重みは

\[
\max_{S\in D}\{
Q(E(S)-E(S_c))+\Phi(S_c)-\Phi(S)
+U_{\rm remaining}(pc,S_c)
\}
\]

とする。既に払ったMP、既に受けたダメージ、既に起きたカウンターを消してはならない。
式の上限が必要なら、検証済みの区間・式で計算する。

例えば、

- 味方と敵の行動が終わり、カメラだけが残るなら、残りの資源増減はすべて0。
- 味方の行動が終わり、敵の行動だけが残るなら、残りでHEALや薬草は使えない。
  ただしアクロのカウンターが可能なら、敵HP減少の可能性は残す。
- HEALのMP2消費まで進んだ後に中断したなら、「HEAL未実行」の場合へ戻してMPを返してはならない。

残りの乱数位置・制御状態は、別途その実行位置からの可能な集合で覆う。
資源変化が0になったことから、乱数位置やカメラも不変とは推論しない。

この補完は、途中まで掘った情報を捨てて一ターン全体の最悪値に戻す方式より弱くならないように作れる。
全体の粗い上限も残し、両方が検証済みなら小さい方を使うこと自体は安全である。
ただし価格探索で本体の凸性を利用する場合は、採用する上限の表現に注意する。
第8節のような**固定された有限個の一次式の最大値**を標準の補完形式とする。
任意の上限同士の最小値を取る拡張では、同じ切除平面法がそのまま使えるとは仮定しない。

一個の補完記録が複数の一次式を持つ場合、その式の数も計算予算に含める。
辺のヘッダ一個に大量の式を隠して、辺数だけ小さく数えない。

**10. 証明木とDP用の辺は、別々に上限を持つ**

詳細な葉がJ個しかなくても、そこへ到達する途中の命令列や中断箇所の数はJとは限らない。
未展開部分をDPの一個の補完辺にまとめても、証明側では各中断領域の被覆を確認する必要がある。
共通祖先で使える一つの補題で覆う場合を除き、中断したpcと領域を勝手にまとめない。

制限する量を少なくとも次の四つに分ける。

```text
K：乱数位置あたりのセル数
J：セル・行動あたりの詳細な辺の項数
C：セル・行動あたりの補完の一次式と行き先集合の項数
V：生成・再利用・独立検査を含む命令/証明ノードの総処理量
```

DPの評価対象の項数は `8 P K (J+C)` 以下とする。
検査記録の総byte数にも別の上限を設ける。
制限に達したら、適用可能な補完へ切り替えるか未判定とする。
証明ノードの途中を黙って省略してはならない。

同じコードの一ターン内の経路長には上限があるが、それを無視して生成費が0だと扱わない。
分割・領域の制限・記号実行の再開を何回行ったかも合計して数える。

**11. 反復で、次に何を直すかを切り分ける**

最大経路が見つかったときの処理を分ける。

| 最大経路の問題 | 次の処理 |
| --- | --- |
| 粗い補完の未実行部分が上限を押し上げる | そのpcと領域だけを再開し、資源の消費・回復の組を保つ |
| 詳細辺同士が異なる実HPや制御状態を仮定してつながる | 食い違う比較の述語を追加し、既存の式を領域制限して接続し直す |
| 資源の収支の価格でまだ改善できる | 同じ検査済み辺で価格を再評価する |
| 正確に接続でき、正本で勝利する | TRUEを返す |

将来の回復を先借りして途中の死亡を免れるような抽象経路は、
総収支の一次式だけでは除けない場合がある。
生存に必要な `A>D` などを、必要な位置でセルに残す。
その述語を直前の更新式に沿って逆算すれば、前の位置で必要なHP条件も得られる。

全状態を探索してから述語を選ぶ必要はない。
一方、少数の述語で必ず十分になる保証もない。
小さいK・J・Cの範囲で証明の余裕が改善するかが、この方式の成否になる。

領域の分割や補完の改善で抽象グラフが変わったら、価格の候補は再利用できるが、
旧グラフから作ったLPの下界を新グラフの下界として使わない。
価格探索の制約は、新グラフに対して作り直す。

**12. 実装前後の判定基準をDPから移す**

実装担当者が最初に確かめるべきなのは、次の三点。

1. 一ターンの証明記録を、固定規則の命令順序に沿って生成・独立検査できるか。
2. 枝や乱数読みを一つ欠落させた記録を、本当に拒否できるか。
3. 小さい領域分割で補完の依存が減り、撃破上限が下がるか。

将来実装する際の、意味のある検査例は以下。

- 非空の真側または偽側を一つ消すと拒否する。
- 必要な比較そのものを飛ばすと、pcの不一致で拒否する。
- 根からFLEEやCRACKの証明を丸ごと消すと拒否する。
- HPの境界243を両方の子から落とすと拒否する。
- RNGの値または読み飛ばし個数を一つ変えると拒否する。
- カウンター後の敵HP式を元に戻すと拒否する。
- 消費済みMPを補完で払い戻すと、適用条件または更新式の不一致で拒否する。
- 出力の一部しか入らないセルを単一行き先と主張すると拒否する。

これはこれから行う検査の指定であり、本書作成時に通過した実測報告ではない。

時間は次の内訳で計上する。

```text
初回の局所記号実行
追加で再開した局所記号実行
ガードの制限と出力先への接続
証明ノードの独立検査
DPと価格選択
再実行による勝利確認
```

最終余裕だけでなく、証明ノード処理数、詳細葉数、補完項数、再開回数、
最大経路上の補完数、追加述語数、再利用した葉の割合を併記する。
DPの2,400万辺評価は、この中の一項の例でしかない。

命令列の準備と監査には開発上の労力が必要で、seedごとの局所証明の生成・検査には実行時間が必要である。
どちらも「既存Stepを呼ぶだけ」として見積もらない。
数百ms〜15秒に収まることは、これらの実測と、小さい分割で証明が締まるかの検証が済むまで未確定である。

**13. 現ブランチの調査結果と、PoCで固定する状態**

調査対象は現行の `BattleEmulator.cpp`、`Player.h`、`BattleInitialPlayers.h`、
`setting.h`、`lcg.cpp`、`camera.cpp` と、開始状態を渡す `main.cpp` の該当箇所である。
Git・他ブランチ・過去の研究レポートは根拠にしていない。

PoCでは、固定能力値の味方一人と敵一体、本体仕様の8行動、現行の敵行動選択に限定する。
敵の選択肢は `VICTIMISER, HP_HOOVER, CRACK_ENEMY, ATTACK_ENEMY, MANAZASHI, PUFF_PUFF`。
内部のカウンター・回避・麻痺解除・行動不能も含めて、一ターンの規則を閉じる。
列挙値がヘッダにあるだけの別の技を、この集合に追加しない。

| 調査した処理 | 現ブランチで使える事実と、その限界 |
| --- | --- |
| `BasePlayers` と `setting` | 敵最大HP456、味方最大HP65、現在MP22。味方atk61・def66・speed40、敵atk56・def58・speed54。薬草の実初期値は7で、`setting::herbcount=8` を代入しない |
| `Main` と `callAttackFun` | 上の到達可能な呼出しに `sleeping=true` の更新がない。両者が眠っていない入力から、眠りは新たに発生しない |
| 能力値の更新 | `RecalculateBuff` は定義されているが、この一ターンの呼出し経路から呼ばれない。対象のatk・def・speedは固定。味方の防御倍率 `defence` は別で、毎ターン1.0へ戻りDEFENCEで0.5になる |
| HEAL・CRACK・薬草 | 実行時にそれぞれMP2、MP3、薬草1を消費する。対象行動にMP・薬草を増やす更新はない。入口の合法性を修正規則で保証する必要がある |
| `ProcessRage` | 敵HP228・114の通過条件は各HPと定数の比較になる。怒りの開始時に残り2〜4を代入する |
| `MANAZASHI` と味方行動開始 | 麻痺を付けると残り4。減算後0以下で解除表を読み、-3の判定は必ず解除になる |
| 必殺・アクロ | 必殺の生タイマーは毎ターン減算、アクロの生タイマーも指定の行動後に減算される。フラグが無効でも負へ減り続ける場合がある |
| `paralysisLevel` | 増加・解除時の0代入はあるが、値を読む `==3` の本体は空。現在の遷移や乱数消費を変えない。生の整数演算の安全性は別途確認する |
| `camera` | Playerを読まず、行動配列・カメラ値から乱数を消費する。先攻引数から挙動を推測せず、実際の行動配列と関数内の処理順を使う |

これらから、**証明用のターン境界の制御型**を次に固定する。

| 座標 | enumの全値 | 値数 |
| --- | --- | ---: |
| Charge | `OFF, ON(0), …, ON(6)` | 8 |
| Paralysis | `CLEAR, ON(-2), ON(-1), ON(0), …, ON(4)` | 8 |
| Acro | `OFF, ON(1), …, ON(6)` | 7 |
| Rage | `OFF, ON(1), …, ON(4)` | 5 |
| Inactive | `false, true` | 2 |
| Camera | `0, …, 5` | 6 |

`ON(0)` と `OFF` は別の値である。麻痺の `ON(-2)` も有効な状態異常である。
フラグをタイマーの正負から復元しない。この直積26,880通りを事前に列挙する必要はない。
一個のBoxなら、六つのmaskで全通りを表せる。

生の状態からこの型への写像を `alpha` とする。無効中のタイマー値は証明用modeから省く。
これは実状態の値を変更する操作ではない。再実行用の状態には生の値を保持する。
可逆な実状態の保存と、この情報を捨てる証明用写像を同一視しない。

縮約の根拠は次の通りで、RuleProgramとの対応表に登録する。

1. 必殺がOFFの間、減算後の `==-1` は既にfalseのフラグをfalseにするだけで、乱数を消費しない。
   再チャージはタイマー6を代入する。アクロ使用は必殺をOFF・生タイマー0にする。
2. アクロがOFFなら、減算後のタイマー値はアクロ失効の追加乱数を発生させない。
   再有効化ではタイマー6を上書きする。
3. 麻痺・怒りの無効中のタイマーは、その状態の処理に使われず、再有効化時に上書きされる。
4. 麻痺は `r→r-1` の後、非正なら表添字 `abs(r-1)` を使う。
   入力の有効範囲-2〜4から、この添字は0〜3。-3では `1.0 >= getPercent(...,100)*0.01` が常に真になり、CLEARになる。
5. 必殺は有効0から次のターン冒頭でOFFへ、アクロは指定の減算で1からOFFへ、怒りは敵行動後の減算で1からOFFへ移る。
   FLEE等でその減算箇所を通らなければ、対応する値を維持する。
6. カメラ0〜5からの各分岐は0〜5へ戻る。`5-counter` の引数も0〜5である。

各mode更新の表は、規則の定数・減算・比較からkernelが8入力値以下を再計算する。
生成器の自由な写像表を信用しない。OFF中の生タイマー等を省いてよいことのC++対応監査は、
第1節のRuleProgram作成者の責任である。その監査まで局所証明から自動的に得られるとは主張しない。
必要な対応は、合法性、資源と乱数位置、次のmode、終端判定について
`alpha(Step_raw(S,b)) = Step_mode(alpha(S),b)` となることである。

生の整数の未定義動作を縮約で隠さないため、入力ごとに残り予算Hを使って検査する。
必殺・アクロの生タイマーには、保守的な下界 `min(入力値,0)-H` がintの範囲内であることを要求する。
麻痺レベルには `max(入力値,0)+H` がintの範囲内であることを要求する。
これらは各ターン高々一回の減算・増加と、固定値へのリセットから得られる。
ターン番号・Geneの添字等も、使う再実行入口の範囲内であることを検査する。

途中局面を受け取る場合、HP・能力値・有効mode等を入力から確認する。
不正な値を近いenumへ丸めない。眠り中、有効麻痺タイマー-3、能力値不一致などは、
このプロファイルの対象外として `UNSUPPORTED_INPUT` を返す。
過去の戦闘開始からの到達可能性を逆に証明する必要はなく、指定された正確なS0からの命題を扱う。

FLEEの修正規則もPoC用に固定する。選択時は麻痺中なら不許可とする。
敵先攻後、実行時に麻痺またはinactiveなら通常の行動不能処理へ進み、解除判定・フラグ更新・必要な乱数を実行する。
行動可能なら味方の逃走選択部分を実行し、敵行動・ターン処理・カメラは維持する。
inactiveによる行動不能処理の適用も、このRuleProgramの明示的な修正点である。
無修正の `Main` を再実行して、この修正規則での勝利手順と呼ばない。

**14. Invariantを作る人と、保証する人を分ける**

v1は、不変条件の探索器を作らない。kernelは入力の最大HP・現在MP・現在薬草数と、
第13節の固定型から、次の一個のBoxを自分で作る。

```text
BaseBox(S0) = E:[1,456], A:[1,65], M:[0,M0], I:[0,I0]
              × 第13節の六つのmodeの全mask

0<=M0<=22。I0は受理したS0の実値で、PoCの標準初期状態では7。
本プロファイルでは0<=I0<=7を受理し、所持数を変更する問題は別の入力プロファイルにする。
```

M0・I0を上限にできる理由は、対象遷移に補充がないことである。
E・Aの下界1は継続状態だけに適用する。成功・失敗終端へは適用しない。
固定能力値・眠りなし・省略フィールドの条件は、mode化のプロファイル検査に含める。
`rage→E<228`、`paralysis→A<...` のような観測由来の相関を勝手に追加しない。

経過ターンtと乱数位置pは、次の有限な外包で管理する。

\[
\operatorname{Envelope}_t=
\{p_0\le p\le p_0+tR_{\max}\}\times\operatorname{BaseBox}(S_0),
\qquad 0\le t\le H.
\]

`Rmax` はダメージ上限ではなく、一ターンの乱数位置の増分上限である。
規則の固定ループと一段のカウンターを展開し、各命令の消費数を足し、分岐では最大を取って求める。
native関数の消費上限も登録し、kernelが同じ有限の制御フローから再計算する。
現コードでは、次の緩い積み上げで64を予算にできる。

```text
ターン冒頭                                     <= 3
味方行動前1 + callAttackFun 20 + 行動後2       <= 23
敵選択前後6 + callAttackFun 20 + 行動後1       <= 27
ターン末尾                                     <= 1
カメラの二枠                                   <= 6
合計                                           <= 60 <= 64
```

ここでcallAttackFunの20は、対象caseとその内部呼出しを含む保守上限である。
到達不能な見惚れ側も含めて選択部分を緩く数えている。
実装では表の64を無条件に信用せず、登録したRuleProgramについて消費上限を再計算する。
規則が変わって64を超えれば、対応するEnvelopeと配列容量を作り直す。
現LCGの配列は5000要素なので、PoCの簡単な入口条件は `p0>=1` かつ `p0+H*Rmax<=4999`。
読み飛ばしも含めて位置を確認する。範囲外の遷移を存在しない辺として捨てない。

**この外包を真だと仮定して根を削るだけでは不十分である。** 合格に必要な義務は次の三つ。

\[
\begin{aligned}
&\alpha(S_0)\in\operatorname{Envelope}_0,\\
&S\in D_{t,q,b},\ \operatorname{Step}(S,b)=S',\ S'\text{が継続}
  \Longrightarrow S'\in\operatorname{Envelope}_{t+1},\\
&S'\text{が終端}\Longrightarrow E',A',M',I'\ge0
  \text{かつ、規則どおりの成功・失敗分類}.
\end{aligned}
\]

第一式は入力の値で直接検査する。第二・第三式は、第5節の全詳細葉と第15節の全補完に対して検査する。
詳細葉では四つの資源像の端点とmodeの像を検査するだけでよい。
MP・薬草が負になる葉を「Invariantの外なので無視」とすると、消費を無視した偽の証明になる。
成功葉についても非負を検査するのは、DPの証明が撃破時の `Phi(S')>=0` を使うためである。

必要な根は、次の前向き手順でkernelが決める。

```text
Support[0] = { (p0, partitionの中でalpha(S0)を含む唯一の葉) }

for t = 0 .. H-1:
    for (p,cell) in Support[t]:
        for b in 固定された8行動:
            D = Envelope[t] ∩ Cell(cell) ∩ Selectable(b)
            if D is empty: continue
            D全体の証明を検査し、全詳細葉・全補完から出力を導く
            継続出力がEnvelope[t+1]に入ることを、交差で削る前に検査
            全出力先の(p',cell')をSupport[t+1]へ追加
            成功・失敗出力は、それぞれの終端へ分類
```

セル一個の支持は、その中の全状態が到達したという主張ではない。
全状態を根として多めに調べることは安全である。最初のセルでも同じ扱いにする。
支持を具体的な生存個体や代表HPから作ることはしない。
補完が広い行き先を要求した場合も、その支持を引き受ける。処理量を超えたら未判定にする。
H層目の継続状態は、その先の行動を検査する必要がない。

この手順についてtで帰納すれば、実際に到達する全継続状態がSupportとEnvelopeに入り、
そこから選べる全行動が覆われる。根・出力のいずれにも到達可能性を判定する未知の関数はない。
乱数位置の外包はtで変わるため、DPの層と対応させる。Hを変えた証明に古い支持集合をそのまま流用しない。
規則実行の葉そのものは第7節の条件で再利用できる。

| 担当 | 責任 |
| --- | --- |
| RuleProgram作成者・レビュー担当 | 現C++、明示した修正点、mode化、省略フィールド、native計算との対応を固定する |
| kernel | 入力検査、BaseBox・Envelope・合法領域の構築、全必要根の決定、保存性と終端資源の検査を行う |
| 証明生成器 | セルの切り方、展開箇所、価格、候補手順を提案する。Invariant・合法性・空判定は上書きできない |
| 呼出し側 | seed・正確なS0・残りターンの意味を固定し、同一問題の証明と手順だけを組にする |

さらに強い条件が必要なら、v1では許可された原子条件でセルを分割する。
新しい不変条件を足して根から状態を削るAPIは設けない。
したがって「良いInvariantを発見できれば動く」をPoCの前提にしない。

**15. 補完も、任意の上限関数にしない**

v1で `COMPLETE` を認めるpcは、次の四種類だけにする。
各pcは、規則に登録された行動後の処理まで終えている位置とする。
生成器が付ける `action_progress` の文字列だけで適用を許可しない。

| 中断箇所 | 残りの資源変化の上限 |
| --- | --- |
| ターン入口。コマンドは合法に選択済み | 第8節の行動別の表 |
| 味方行動とその行動後処理が終了し、敵行動が未実行 | 敵HP減少はカウンターによる最大64、味方HP増加なし、MP・薬草増分0。従って `64Q` |
| 敵行動とその行動後処理が終了し、味方行動が未実行 | 下の味方行動だけの表 |
| 両者の行動とその行動後処理が終了し、カメラ等だけが残る | 資源増分はすべて0 |

敵行動後、味方行動だけが残る場合の表は次の通り。

```text
ATTACK                 : 64Q
DRAGON                 : 34Q
DEFENCE / FLEE / ACRO   : 0
CRACK                  : max(0, 34Q - 3v)
HEAL                   : max(0, u*gH - 2v)
薬草                   : max(0, u*gI - w)
gH=65, gI=39 は第8節と同じ保守上限。
```

各 `max` は、未実行・実行の二つの資源収支ベクトルを明示的に持つという意味である。
補完書式を `cut_id, case_id, target_kind` とし、上限係数と出力外包はkernelが表から再構築する。
任意の式や任意の補題関数を証明書から読み込まない。
そのcutで必要な全case_idもkernelが列挙し、一つでも欠ければ拒否する。
caseを省略できるのは、kernelがその中断領域で不可能だと確認した場合だけである。
具体的なpcで既に生死が確定している場合は、まず終端として処理し、残りの攻撃機会を追加しない。

この表だけでは、出力がInvariant内にあることの根拠にならない。
補完には次の出力外包も必須であり、kernelが残りの処理の性質から作る。

1. 敵HPの下界は `max(0, 現在Eの下界 - 残り直接攻撃上限 - 残りカウンター上限)`。
   敵行動が残る場合の上界は456、味方行動だけなら現在Eの上界。
   HP_HOOVERによる回復を無視して上界を小さくしない。
2. 味方HPの下界は保守的に0。上界は回復実行の場合65、回復しない場合は現在Aの上界。
   残り資源変化が0の箇所では、E・Aとも現在の式をそのまま保つ。
3. MP・薬草は、対応する実行ケースでだけ現在の式からコストを引く。
   未実行ケースや、既に味方行動が済んだ箇所では現在の式を保つ。
   コストを引くケースでは、その中断領域全体で残量が足りることを検査する。
4. 制御値は第13節の各更新表の像を含める。粗い場合は全modeを使える。
   根拠は、HPのclampと非負ダメージ、各資源の書込箇所、有限modeの更新表であり、
   「出力もInvariant内のはず」という仮定ではない。
5. 乱数位置は `[pc時点のp, pc時点のp + Rremaining(pc,stack)]` で覆う。
   `Rremaining` は第14節と同じ、命令の消費数と分岐maxから求める。
   確定したprefixの消費と足して一ターンの上限に入ることも検査する。

Eの出力下界が正なら成功終端を行き先に加える必要はない。
下界が0なら、撃破不能と別途証明していない限り成功終端を含める。
生存する出力を分類するときだけE・Aの正の部分を取り、E=0の成功部分、E>0かつA=0の失敗部分も覆う。
資源が負になる部分を、この終端分類の名目で捨ててはならない。

補完の重みは第9節どおり、検査済みprefixの正確な収支上限と、上表の残り上限を足す。
従って、敵先攻のカウンターをもう一度与えたり、支払い済みのMPを戻したりしない。
HEALのMP消費直後等、表にない途中pcでの `COMPLETE` はv1では拒否する。
そのpcで予算に近づいたら、許可されたpcまでの処理を予約できる場合だけ進む。
予約できなければ未判定とする。任意pc用の補完エンジンをPoCの前提にしない。

行き先集合の書式も限定する。

```text
詳細葉       : kernelが出力分割木の逆像で求めた単一の(p',cell')、または終端
補完         : 検査したp区間にある全セル + 必要なら成功終端
最も粗い補完 : 次層の全セル + 必要なら成功終端
```

補完のp区間は出力外包全体を含む必要がある。
その区間の外や、未登録のセルへ出る可能性を黙って削れない。
全セル最大は層ごとに一回求める。p区間最大を使うなら、まずpごとの全セル最大を作り、
固定配列の区間最大表等で問い合わせる。その準備費・容量・問い合わせ数も計上する。
例えばsparse tableなら層ごとに `O(P log P)` の準備と容量が必要であり、DPの辺評価数に隠さない。
複数の一次式・行き先集合の組は、実際の組数をCに数える。

PoCでのRuleProgram登録検査には、各caseの資源の書込先・消費回数・ダメージ上限、
mode表、pc別の行動済み情報と乱数消費上限を含める。
第13節の「生のタイマー値を省略してよい」という監査はこの登録の一部であり、
候補生成器の証明書から追加できる公理にはしない。
未登録の書込み・呼出し・caseが増えた場合は、この補完を再利用しない。
通常の詳細な記号実行へ進むか、そのプロファイルでは未対応にする。

**16. d(N−1)=偽 と d(N)=真 を組にして、時間を測る**

以後のNは、指定された正確なS0から最初の撃破までのターン数である。
観測済みのt0ターンを足して表示する場合は `t0+N` とし、固定された観測条件下での最短性と明記する。
この局面の結果を、別の開始局面や未固定の前半手順まで含む最短性へ一般化しない。

最短性の証明書は、次の二つと共通の入力識別情報を持つ。

```text
Problem = (rule_id, 数値計算の設定, seed, 正確なS0, 観測・行動制約)
TRUE    = Nターン以内のコマンド列と、同じ規則での再実行結果
FALSE   = H=N-1、セル分割、被覆・補完、整数価格、DP不等式
```

TRUE側は、各ターンの選択合法性、敵先攻後の実行時状態、乱数位置、最初の撃破を検査する。
近似したHPやmodeの代表値による再実行では合格にしない。
FALSE側は、第14節の被覆と保存性を検査した上で、

\[
\delta=QE_0-\bigl(B_{N-1}(q_0)+uA_0+vM_0+wI_0\bigr)\ge1
\]

または抽象成功経路が存在しないことを整数で確認する。
敵HPが0なら初めからN=0の自明な成功として扱い、`d(-1)` を問い合わせない。
味方死亡・H=0等の自明な偽も、同じ終端優先順位で検査する。
この二つがそろったときだけ、正確な問題について最短Nと返す。

近似と量子化の許可箇所を、次のように分ける。

| 箇所 | PoCでの扱い |
| --- | --- |
| 戦闘のHP・MP・乱数・ダメージ | 固定した規則の正確な値と演算を使う |
| HP帯やmodeのまとめ方 | 第2節のBoxによる外側近似。代表点へ丸めず、帯の全点の遷移を覆う |
| 証明価格 | 量子化してよい。初期設定は `Q=256`、`0<=u,v,w<=2^20` の整数 |
| 価格候補の探索 | 非信用部分なので浮動小数や小さいLPを使える。丸めた係数で必ず整数DPを再評価する |
| 最終判定 | 整数の厳密比較。浮動小数の微小な負の余裕、LPの終了表示、探索失敗を証明として受け取らない |

固定分母が粗くて余裕を失うなら別のQを候補にできるが、積・和を検査し、DPを再評価する。
価格候補の最適性を証明する必要はない。一組の係数で上の十分条件が成立すれば足りる。
浮動小数で求めただけのLP下界から、価格の族全体が不十分だと断定もしない。
本体仕様の厳密な有理数LPは拡張として残し、PoCの必須部分にはしない。

動作の順序は次に固定する。

```text
入力受理から共通のdeadlineを開始する
既知の候補手順があれば、修正規則で再実行して最初の撃破長Nを得る
候補がなければ、候補生成器や抽象経路から手順を求め、同じ再実行を行う

勝利手順が得られたら H=N-1 の否定証明を作る
  整数の否定証明が検査に合格 → 手順と組にして OPTIMAL(N)
  より短い実手順が見つかる   → Nをその撃破長へ更新し、新しいH=N-1を調べる
  上限が締まらない           → 第11節の分割・再開・価格更新を予算内で続ける
  時間・容量不足             → 検査済みの上界・下界とUNKNOWNを返す
```

手順探索で見つからなかったことを偽の根拠にはしない。
既に `d(h)=偽` を証明し、長さNの勝利手順もあるがh=N-1まで届かなかった場合は、
`h+1<=最短ターン数<=N` を返せる。最短確定とは表示しない。
観測条件・seed・S0・RuleProgramが異なるTRUEとFALSEを組にしてはならない。

数百ms〜15秒を狙うPoCの開始設定を、次の累積予算とする。
これは実測値でも達成の保証でもなく、実装後に調整する初期の打切り設定である。

| 入力からの累積時間 | K上限 | J上限 | C上限 | DP評価する価格候補の累積上限 | 総ノード・領域処理Vの累積上限 | 記録byte上限 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 300ms | 2 | 2 | 2 | 2 | 50万 | 16MiB |
| 2秒 | 4 | 4 | 3 | 4 | 200万 | 64MiB |
| 15秒 | 8 | 8 | 3 | 8 | 800万 | 128MiB |

実際の詳細・補完項数にも、順に6.5万・15万・25万の総上限を置く。
K・J・Cは上限であり、全乱数位置で使い切る指示ではない。
RNG位置数Pは `H*Rmax+1` 以下の外包から支持を絞り、Hを20に固定しない。
価格候補数は、グラフを変更しても累積予算をリセットしない。
節目で証明できたら直ちに返し、最終段階まで必ず反復する必要はない。

各処理は小さい単位で予算を確認し、最終検査と手順再実行に必要な時間を残す。
最終検査を打ち切った場合、その候補はFALSEとして受理しない。
同じkernel実行内で独立検査済みの葉は再利用できるが、生成器や以前の別実行の「検査済み」印は信用しない。
決定的な連続命令はまとめて走査してもよい。記録の圧縮で各命令の処理数Vまで減ったことにはしない。

時間報告には、入力・乱数テープの準備、手順取得、局所証明の生成、kernel検査、
領域接続・支持集合・区間最大表、全価格のDP、最終再実行を含める。
キャッシュなしの時間と再利用時の時間を分け、H・P・実セル数・実項数・V・総byte数を併記する。
コード中の古い手順コメントや、入力・seedが未確定の観測例を、検査済みの最短性データとしない。

実装時の追加の拒否検査は、次に絞る。

- BaseBoxから合法なMP値やmodeを削ろうとする証明書を受理しない。
- 生の無効タイマーが異なる二状態は、定義域内なら同じmode遷移になる。麻痺の負の有効タイマーはCLEARと区別する。
- 出力をEnvelopeで切り落とす証明、成功時にMPが負の証明を拒否する。
- 補完の行き先に含まれるセルをSupportから一個消すと、必要な根の欠落として拒否する。
- 許可されていない途中pcの補完、済んだカウンターの追加、消費済みMPの払い戻しを拒否する。
- 量子化前だけ不等式が成立する候補や、別の入力・規則の勝利手順との組を拒否する。

PoCの合格条件は、正確な入力ごとの `d(N-1)=偽` と `d(N)=真` の組を、
予算内で独立検査できることである。判定できた問題の割合と、未判定の理由も測る。
今ある資料だけでは、この組や300ms〜15秒の達成を報告する段階にはない。
必要なのは、以上の固定形式で小さい証明が実際に作れるかという実測である。

**17. パラメータを追加する場合の境界だけ残す**

他ブランチの調査・実装は本PoCに含めない。以下は利用者から示された将来要件への接続点である。
眠り、ボミオス、ルカナン等は、正の残り期間に続いて、0以下を解除カウンタとして使い、
将来的に `T,...,0,-1,...,-4` のような範囲を取りうる。
この場合も `CLEAR` と `ON(r)` を別の値にし、0や負を無効状態へまとめない。
最終負値で必ず解除するか、さらに判定があるかは、その対象規則から検証する。

追加時に必要なのは、初期値、全書込箇所、有限な継続・解除の更新表、
行動不能や乱数消費への影響を登録し、同じ入力・保存性検査に通すことである。
値数が8を超えるならmaskと小さい写像表の型を広げ、その処理量もVへ加える。
有限性の根拠がなければ、適当な範囲へ切り詰めて受理しない。

守備力・素早さのバフでは、段階とタイマーを制御座標に追加する。
ダメージ計算・先攻判定で実効能力値を使う直前に必要なmodeを分割し、
具体的なatk・def・speedで既存の限定計算を呼ぶ。
資源が引き続き各自の入力座標の平行移動・clampだけなら、第2節のBoxを拡張して使える。
現在HPに比例するダメージ、資源同士の転送などが増えれば、そのままでは同じ形式に閉じないため別の拡張が必要になる。
ダメージ・回復上限、補完表、RNG消費上限、RuleProgram識別子も変更時に再検証する。

初版は本ブランチの六つのmodeと四資源で、最短性の組と実用時間を確認する。
