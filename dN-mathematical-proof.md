**有限領域と近似から最短Nを証明するための数学的条件**

本書は [本体仕様](d20-worldline-proof-spec.md) と [kernel仕様](d20-proof-kernel-spec.md) の健全性を証明する。
特定seedの最短Nや実行時間の実測を報告するものではない。追加のソース調査・実装は行わない。

**1. 対象と仮定**

seed、正確な開始状態S0、開始ターン、合法な選択集合、実行時の修正規則、数値計算を固定する。
経過tの一ターン遷移を `T_t(S,b)` とする。敵の行動回数が奇偶で変わる場合も、このtから決まる。
成功は敵HPが0、失敗は敵HPが正かつ味方HPが0とする。

資源を敵HPのEと、非負のベクトル `r=(A,M,I1,...,Ik)` に分ける。
成功・失敗終端でもrの非負を検査する。
無効タイマー等を省く写像alphaは、合法性・資源・乱数位置・制御状態・終端について

\[
\alpha(T_t(S,b))=\overline T_t(\alpha(S),b)
\]

を満たすことが登録条件である。以下はこの条件を満たす固定RuleProgramに対する証明である。
C++からRuleProgramへの対応やnative関数の契約を、証明生成器の宣言だけで保証したことにはしない。

**2. 補題：限定した領域演算は、代表点実行を必要としない**

一領域Dを、各資源の整数区間と有限modeの部分集合との直積とする。
一葉の資源更新は各自の入力座標について `x'=x+c` または `x'=c`、
mode更新は同じ入力modeの有限写像または定数とする。
他の座標に依存する更新は、その条件で分割してこの形にした後に扱う。

このとき、単一座標の閾値・mode所属条件による制限、領域の共通部分、像、
同形式の出力セルの逆像は、区間の端点と有限集合だけで正確に計算できる。

証明。`x+c<=a` の逆像は `x<=a-c`、定数式なら全域または空である。
有限写像fの条件 `f(s)∈U` の逆像は、入力modeを調べた集合 `{s | f(s)∈U}` である。
各更新が自座標だけに依存するため、像も各座標の像の直積になる。
閾値分割の真側と偽側は互いに素で、その和集合は元のDに一致する。
複数セルの集合は分割木の複数葉として扱えばよく、全資源値の列挙は不要である。□

すべての端点計算はオーバーフローを検査する。
この補題は資源間の転送や任意の線形制約へ自動的には拡張しない。

**3. 補題：負の解除カウンタは有限modeとして閉じる**

指定先端で確認した解除処理について、乱数値rhoが0〜99、
残り値が付与時に1以上T以下へリセットされ、減算後-3では必ず解除されるとする。
継続中の候補値域を `ON(r), -2<=r<=T` と、別の値OFFにする。

証明。減算を行わなければ同じ値、再付与なら1以上T以下である。
減算を行う場合、r>=-1なら `r-1>=-2` なので、解除しなくても候補値域に入る。
r=-2からは-3になるが、バフの最終条件は `100>=rho` であり、必ずOFFになる。
眠りの最終条件も同じである。従って有効な-3を次の境界へ持ち越さない。
一回の減算イベントがこの型を保存するので、一ターンに二回起きても合成は型を保存する。□

ここで数えたのは減算イベントである。毎ターン一回減るという仮定は使っていない。
OFF中の生タイマーは別途整数安全性を検査し、-4以下をONの値だと解釈しない。
将来ON(-4)まで続く規則では、その規則の最終解除条件から型を作り直す。
段階の上下限については付与caseと行動選択を含む保存検査が別に必要であり、
タイマーの補題だけから `level>=-2` 等は導けない。

**4. 定理：kernelの局所検査から、全到達遷移の被覆が得られる**

kernelが構築する継続外包をA_t、セルの分割をC_q、合法な選択領域をL_bとする。
検査する根は `D=A_t∩C_q∩L_b` である。次を合格条件とする。

1. `alpha(S0)∈A_0`。
2. 必要な根について、kernel自身が規則の次pc・全非空分岐・更新を計算する。
   通常葉は規則の終了位置だけで認め、補完葉は検査した残り遷移の外包を持つ。
3. 全継続出力がA_(t+1)に入ることを、出力を交差で削る前に検査する。
4. 支持集合は初期セルから始め、検査済みの全継続出力先を次の支持へ加える。
   必要な根または非空caseが欠ければ拒否する。

証明。まず一つの根Dの任意の点sを取る。
決定的命令ではkernelの再計算によってsの具体実行との対応が保たれる。
比較では補題2によりsは真側か偽側のどちらかに必ず入り、その子は必須である。
従って規則の有限な命令列を進めても、sが証明から失われることはない。
通常葉なら正確な出力、補完葉なら検査済みの外包がsの実行を含む。
これで一つの根の全入力が被覆される。

次に経過tで帰納する。t=0は条件1と初期支持から成立する。
到達状態sがA_tと支持セルに入ると仮定すると、任意の合法bに対応する非空根が必須になる。
先ほどの局所被覆と条件3・4から、その継続出力はA_(t+1)と次の支持に入り、
終端出力も成功・失敗として覆われる。よってHまでの全合法実行が覆われる。□

この帰納法は生成器を信用せず、到達可能性を返す未知のInvariant関数も使わない。
敵先攻のMP吸収後にMPが負になる等、保存検査に失敗するモデルを、
「外包の外なので無視」として受理することはできない。

**5. 定理：外側近似による d(H)=偽 と、最短Nの証明**

正整数Qと非負整数ベクトルlambdaを固定し、`Phi(S)=lambda・r(S)` とする。
定理4の被覆から導いた各抽象辺tauが、対応するすべての実遷移について

\[
W_\tau\ge Q(E-E')+\Phi(S')-\Phi(S)
\]

を満たす整数上限W_tauを持つとする。B_HはH以内に成功へ至る抽象経路の重み和の最大値とする。
成功だけを0、その他を負の無限大で初期化したmax-plus DPは、経路長での帰納法によりこの最大値を計算する。
実行ターンに依存する辺では、各層に対応するT_tの辺を使う。

証明。H以内に勝つ実経路 `S0,...,Sk` があると仮定する。
定理4より、これを覆う抽象成功経路がある。その辺不等式を足すと、

\[
\begin{aligned}
B_H(q_0)&\ge\sum_{i=1}^{k}W_{\tau_i}\\
&\ge Q(E_0-E_k)+\Phi(S_k)-\Phi(S_0)\\
&\ge QE_0-\Phi(S_0).
\end{aligned}
\]

最後は `E_k=0` と `Phi(S_k)>=0` による。従って

\[
\delta=QE_0-B_H(q_0)-\Phi(S_0)\ge1
\]

なら矛盾し、`d(H)=偽` である。抽象成功経路自体がなければ、その場合も偽である。□

さらに同じ問題の正確な再実行がNターンで初めて撃破し、`H=N-1` で上の否定証明が合格すれば、
最短値はN以下かつN-1より大きいので、ちょうどNである。
N=0は初期撃破の自明な場合として別に扱い、`d(-1)` は使わない。
抽象成功経路が存在することだけではTRUEを証明しない。

**6. 系：価格や上限を量子化しても、方向を守れば証明できる**

価格lambdaは非負整数、Qは正整数なら、生成器のどの選択にも定理5を適用できる。
浮動小数の候補thetaを `lambda/Q` へ丸めた場合、その係数で辺とDPを再計算し、整数のdeltaを確認すればよい。
元の価格の最適性や、丸め前の浮動小数の判定を信用する必要はない。

量子化幅による損失も評価できる。価格を変えても固定した抽象グラフで、
各辺の上限が有限個の一次式の最大値として表せる場合を考える。
抽象成功経路が一つ以上あるとし、Pは長さH以下の経路と各辺のケースを選ぶものとする。
ケースの敵HP減少上限を合計した値をD_P、資源増減をDelta r_tauと書き、

\[
U(\theta)=\max_P\{D_P+\theta\cdot c_P\},\qquad
c_P=\mathbf r_0+\sum_{\tau\in P}\Delta\mathbf r_\tau
\]

とする。これはダメージ単位のDP上限に初期価格 `theta・r0` を加えた、定理5の比較対象である。
各座標で `|Delta r_tau,j|<=b_j` を全ケースから検査すれば、
`|c_P,j|<=L_j=r0,j+H*b_j` である。
各経路の一次式の差を三角不等式で抑え、最大値を取ると

\[
|U(\widehat\theta)-U(\theta)|
\le\sum_j L_j|\widehat\theta_j-\theta_j|
\]

を得る。非負の1/Q格子の最近点へ丸めたとき、各誤差は高々1/(2Q)である。
係数上限を設ける場合、この誤差評価には元の価格もその範囲に入ることが必要である。
従って厳密に分かった余裕 `epsilon=E0-U(theta)` が `sum(L_j)/(2Q)` より大きければ、
その価格量子化でも余裕は正に保たれる。実際の受理は丸め後の整数DPで行う。

任意の整数幅b>=1で、辺の上限自体を

\[
\widehat W_\tau=b\left\lceil W_\tau/b\right\rceil
\]

へ量子化することも安全である。負の重みでも数学的な天井関数を使う。
`0<=widehat W-W<=b-1` だから、有限な成功経路がある場合、

\[
B_H\le\widehat B_H\le B_H+H(b-1).
\]

左側から量子化後も定理5の上限条件が保たれ、右側から失う余裕の上限が分かる。
従って元の整数余裕がH(b-1)より大きければ、上限の量子化後も偽を証明できる。□

上限の量子化は追加で選べる方式であり、PoCの必須機能にはしない。
HP帯による量子化は、その帯の全状態を領域に含めて定理4を満たすことで正当化する。
HPの代表値で一回実行することや、ダメージ・乱数そのものを近似式へ交換することは、この系の対象ではない。

**7. 何がまだ実測事項か**

この証明系は健全であるが、固定した小さいセル分割と価格だけで、すべての偽の問題を証明できるとは限らない。
例えば架空の抽象成功経路Pが `D_P>=E0` かつ全座標で `c_P>=0` を持つと、
非負のどの価格でも `U(theta)>=D_P+theta・c_P>=E0` となり、価格だけでは否定できない。
実際にはつながらない中間HP・バフ・予約行動等の条件を、分割や補完の詳細化で除く必要がある。

どの分割ならN-1の上限が締まるか、証明生成と独立検査を数百ms〜15秒に収められるかは実測事項である。
予算切れではUNKNOWNを返し、定理の前提が検査できたときだけ否定証明または最短Nを返す。
