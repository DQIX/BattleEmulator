# nusisama2_new_arugo 最終引き継ぎ
Latest recommended ZIP: /mnt/data/nusisama2_search_final.zip
Final ZIP: /mnt/data/nusisama2_search_final.zip
Latest checkpoint ZIP: /mnt/data/nusisama2_checkpoint_02.zip
Previous stable ZIP: /mnt/data/nusisama2_checkpoint_01.zip
対象branchはnusisama2_new_arugo。目的Xは、与えられた現在状態から固定時間内に、敵HPを0にする正確な行動列を探索し、BattleResult.positionを改善することである。入力prefixは固定し、初期Playerをそのまま探索rootにしてはいけない。指示書ではローカルMCP、personal_context、Web検索、新規テストコード、サニタイザおよび一般的なメモリ整合性検査が禁止されている。今回これらは使用していない。元のA*のweightは別BattleEmulator向けの流用なので、修復の主目的にはせず参考baselineとして温存した。
NusisamaSearch.cpp/.hを新設し、main.cppの既存SearchRequest署名を維持したまま新探索器へ接続した。旧SearchRequestはSearchRequestLegacyとして保持。CMakeと既存WASMビルドのソース一覧へ新ファイルを追加した。WASM exportや入力パーサー、敵AI、Player、ダメージ式、乱数処理、合法action集合は変更していない。参考にしたのは添付Git内のreokonn-GPT6-Pro、bilyouma-gpt6-pro、yo2-gpt6-proであり、compact path arena、exact照合付き重複排除、段階的beam拡幅、戦術phase quota、suffix repairを利用した。回答action列を探索ソースへ埋め込んでいない。
このbranchのMainはnowStateに保存された実ターンで全体action配列を参照する。1技だけの小配列を渡す他branchの実装をそのまま使わず、該当実ターンに技を設定してMainで1ターン進める。採用候補は初期状態からprefixとsuffixを全再生し、全Player field、nowState、RNG cursor、BattleResult.positionを内部状態と一致確認する。さらにsuffixを1ターンずつ再生し、合法性・FLEEの状態異常回避禁止・ためるの既存事後条件・全BattleResultレコードを完全再生と照合する。
13variantの比較まで完了した第2checkpoint。development.txtの8入力中6入力とdevelopment-extended.txtの20入力中13入力が生存rootであり、新方式は全件勝利した。残り9入力は固定prefix中に死亡しており、探索失敗率へ混ぜずeligible=0として記録した。development-live.txtは探索結果を一切参照せず、汎用準備prefixを再生して生存rootだけを生成した20入力である。prefixは0、1、3、6、8、10ターンを含む。旧6表A*は250msで17/20勝利、新方式のvariant0/2/3/6/7は20/20勝利、平均position=47.05で同値だった。25msではvariant6（survival）が20/20勝利、平均position=47.05で最良。他の有力variant0/2/3/5/7は47.20だった。全採用候補の正確リプレイ照合が成功し、rejectedReplay=0。
## 最終採用と完了状態
上記の第2checkpointから作業を完了し、現在は本番DefaultVariant=6（survival）、DefaultBudgetMs=1000へ変更済み。development-liveの1000ms比較でもvariant0/2/6は平均position47.05で同値、variant6は実行平均502.3756ms、最大804.6940msだった。最大beam幅16384まで終わると予算を使い切らず返却する。25msでの解品質を理由にvariant6を採用固定し、FROZEN_SELECTION.mdとfrozen-source.sha256を保存した。その後、開発入力とseedが重複しないholdout20入力を作って評価した。holdoutの結果を見た再調整はしていない。実装、本番接続、開発比較、独立holdout、正確リプレイ、REPORT、ZIP保存まで完了した。
## 独立holdout結果
holdoutはprefix0/1/3/6/8/10ターンを含む生存root20入力。variant6は25/250/1000msすべて20/20勝利、平均BattleResult.position47.30だった。250msで旧6表A*は19/20勝利、新方式は7件改善・12件同値・1件追加勝利・悪化0件。両者勝利19入力の平均positionは旧49.8947、新46.5789。1000msでは両者20/20勝利、旧平均50.20から新47.30へ改善、7件改善・13件同値。variant6の250ms平均初勝利は1.7159ms、実行平均241.9828ms、最大247.1860ms。1000msの実行平均504.5030ms、最大735.4740ms。全holdout採用結果のexact replayが一致し、rejectedReplay=0、variant6の予算超過0件。評価集合内の最小positionは37（seed0x19b5a8、prefixなし、13ターン）だが、全域最適性は証明していない。
## 代表解と提出内容
固定10ターンからの代表解はseed0x3741eb、prefix「30 30 62 37 62 37 62 33 37 34」、探索suffix「30 62 37 33 62 62 62 34」。250msで全18ターン、position52、最終味方HP75、敵HP0、RNG cursor921、nowState73728。evaluation/results/representative-prefix10.txtに既存dumpTableと結果を保存した。既存dumpTableは各イベントのダメージ適用前HPを表示するため、最終行に敵残HPが見えても最終Player状態とは別である。本番SearchRequestも実行し、seed0x193e27、prefix30からposition43の15ターン勝利をproduction-SearchRequest.txtへ保存した。
最終ZIPは/mnt/data/nusisama2_search_final.zip。完全な変更後ソースtreeと元.git、全13variant、評価コード、固定入力、全CSV、集計、dumpTable、REPORT、SEARCH_USAGE、HANDOFFを含む。参考branchはreokonn-GPT6-Pro、bilyouma-gpt6-pro、yo2-gpt6-proで、各参照commitはREPORTに記載。対象元HEADは4379054a70c432dc41222f6899fd3c8e8c934d3e。元の世界側ファイルがバイト一致することを確認し、unchanged-original-files.sha256も保存した。commit/pushやローカルMCP操作は行っていない。生成binaryとbuild cacheはZIPへ含めない。
## 再現と残る範囲
CLIは--search SEED BUDGET_MS VARIANT [PREFIX_ACTION ...]。ビルドはcmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DNUSISAMA_BUILD_EVALUATION=ON、cmake --build build -j2。benchmarkはnusisama_benchmark CASES.txt BUDGET_MS VARIANTS_CSV OUTPUT.csv。--request SEED [PREFIX...]は本番SearchRequestを呼ぶ。SEARCH_USAGE.mdとevaluation/reproduce.shに一通りの再現手順を保存した。
今回はLinux/GCC14.2.0、C++17、Releaseの-O3、単一スレッドで計測した。Windows/MinGW/MSVCとWASM上の実測は未実施。prefixと絶対horizonは最大90ターン、既定horizonはprefix+40である。試した有力方式は250msと1000msで同じ解品質へ収束したため提出したが、完全探索や数学的最適性証明ではない。採用後に追加改善する場合は新しい開発入力とholdoutを分離し、今回のholdoutへ繰り返し最適化した数字を独立評価と呼ばない。現在の完成成果を利用するために追加作業は必須ではない。ファイルはUTF-8で読み、変更にはapply_patch、sandbox検索にはrgを使う。新たな成果を得たらsandboxの継続を仮定せずZIPを保存し、絶対パスを引き継ぐ。