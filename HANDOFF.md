# 完了引き継ぎ：reokonn_lv8_new_arugo
対象branchはreokonn_lv8_new_arugo。目的Xは元の世界、prefix、合法action、RNGを固定したまま、同じ時間で正確な勝利のBattleResult.positionを改善することである。元のActionOptimizerをそのまま保持し、独立新規ReokonnSearch.cpp/.hへ探索器を実装した。Player、setting、BattleEmulator、LCG、camera、元の探索主要ファイルは入力時ハッシュと一致している。ローカルMCP、personal_context、Web検索は使用していない。テストsuite、sanitizer、メモリ検査は実施していない。
24種類のdevelopment variantを比較し、最終採用はvariant16のgreedy-attack-preview。親リンク式履歴、全意味項目比較による重複排除、64から32768までの段階的ビーム拡張、正規Mainによる次の通常攻撃の1手予測を使う。予測は順位専用で探索stateを改変しない。薬草・HEAL・CRACKの条件も元のまま。FLEEは合法一覧に残すが睡眠・麻痺時のpre-action不正回避はbaselineと新方式の双方で不採用にした。
元のPlayerと固定prefixを完全再生して現在rootを作り、suffixのみ探索する。候補ごとに元の入力から完全再生し、全Player項目、RNG位置、nowState、BattleResult.positionが探索内と一致する勝利だけを採用する。実positionが主要評価でありRNG位置と混同していない。
最終development20ケース・1000msは17対15勝、8改善・7同等・0悪化・新方式のみ2勝・双方未発見3件。ここで方式と時間管理を固定し、evaluation/frozen-search.sha256を保存後に独立holdoutへ進んだ。holdoutを見て探索を再調整していない。40ケース・250msは37対26勝、12改善・14同等・0悪化・新方式のみ11勝・baselineのみ0勝・双方未発見3件。1000msは37対32勝、6改善・26同等・0悪化・新方式のみ5勝・baselineのみ0勝・双方未発見3件。prefix長0/1/3/6/9を8ケースずつ含む。160実行すべて完全再生と逐次再生が一致し、候補のreplay rejectは0。
両方式に同じ予算と10%の終了余裕を適用した。新方式の最大実測時間は250ms指定で230.499ms、1000ms指定で906.230ms。baselineは250ms case28だけ264.899msを要した。この超過を隠さず勝利として集計しREPORTに明示した。1000ms baseline最大は971.945ms。平均positionと初回発見時間の比較は、異なる勝利集合による偏りを避け双方勝利の共通集合で計算した。
代表解はholdout case13、250ms、seed0x1b2905、prefix6手。baseline position55・28手から新方式position49・25手へ改善。敵HP0、味方HP6、MP4、薬草2、RNG位置1006、nowState0x19310。既存dumpTableはevaluation/results/representative.txt、比較側はrepresentative-baseline.txt。これは特定入力の実測例であり全入力の最良値ではない。別のcase6ではposition33も出たが、異なる入力間の小ささを同条件改善と混同しないこと。
native SearchRequestとWebAssembly側buildDumpOutputは既定1000ms・variant16へ接続済み。完全再生済みログを既存dumpTableへ直接渡す。未発見はprefixと未発見概要を返し、不足actionを通常攻撃で補って勝利したことにはしない。native Releaseビルドと実SearchRequestの勝利・未発見の実演は完了した。WebAssemblyソースリストも更新済みだが、Emscriptenがないためwasmビルドとブラウザ実行は未確認。Windows/MinGW実行も未測定である。
実装と固定版holdout評価は完了しており、次に必須の探索作業はない。再開時はREPORTのコマンドを使う。holdoutは観測済みなので今後調整に使うなら別の独立holdoutを確保すること。既定variantは16、0は比較したportfolio-repairとして残す。全variant、比較コード、未加工CSV、dump、元ファイルとハッシュを同梱。入力Git履歴も保持し、巨大build cacheは除外した。理論最適性の証明や一般品質保証を本作業の追加完了条件にはしていない。
Latest recommended ZIP: /mnt/data/reokonn_lv8_optimized_final.zip
Latest checkpoint ZIP: /mnt/data/reokonn_checkpoint_03.zip
Previous stable ZIP: /mnt/data/reokonn_checkpoint_02.zip
Final ZIP: /mnt/data/reokonn_lv8_optimized_final.zip