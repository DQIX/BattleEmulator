**d(20)=偽を判定するための証明仕様 — GPT5.6 solへの引き継ぎ**

2026-09-05。これは設計と数学的な十分条件の証明であり、探索プログラムの実装ではない。

入力資料は `deep-research-report-cleaned-ms.md` と `deep-research-report-cleaned-ms(1).md`。
戦闘の意味は、現時点の `BattleEmulator.cpp`、`Player.h`、`camera.cpp`、`lcg.cpp`、初期値の定義を基準とする。
旧探索、`EnhancedHashCalculator.cpp`、`ActionOptimizer.cpp`、Git履歴は使わない。

**提案の要点**

ホテルに泊めるものを、到達した個体から「この条件では撃破できないことを示す不等式」に変える。

乱数位置ごとにHP・MP・薬草の異なる個体を増やす代わりに、
残りターン数とホテルの部屋ごとに、原則として符号付き64bit整数を一つ保存する。
実際のHP・MP・薬草数を丸めることはない。
これらは不等式の変数として正確に扱う。

最後に確認する形は、次の一つである。

\[
\boxed{
Q E_0>
B_H(q_0)+u A_0+v M_0+w I_0
\quad\Longrightarrow\quad d(H)=\mathrm{偽}
}
\]

ここで、`E` は敵HP、`A` は味方HP、`M` はMP、`I` は薬草数。
`q` は乱数位置と部屋、`Q>0`、`u,v,w>=0` は証明用の整数係数。
`B` の定義、計算、検査方法を以下で与える。

この方式では、ターン間で資源と制御状態の細かな対応を忘れる近似を使う。
その近似で生じる楽観的な可能性まで上限へ含める。
したがって、不等式を検査できた場合は、近似を使って実際の不可能性を証明できる。
経路のサンプリングだけから不可能性を推定する必要がない。

ただし、この上限が対象の20ターンで十分に小さくなること、全計算が5〜10秒で終わることは未実測である。
本書が証明するのは「この条件が満たされたら偽である」と「計算対象の大きさを履歴数から切り離せること」。
特定局面について、その条件が既に満たされたとは主張しない。

**1. 何を判定するか**

固定seed、観測済み入力を反映した正確なターン境界状態を `S0` とする。

\[
d(H\mid S_0)=
[\exists\text{合法な行動列 }a_1\ldots a_k,
\ k\le H:\ E(S_k)=0].
\]

`H` はその局面からの残りターン数である。
戦闘全体の現最良手順が `L` ターン、観測済みが `t0` ターンなら、
その一つ下を調べる残り予算は `H=L-1-t0`。
戦闘開始からの `d(20)` と、途中局面から20ターン使う判定を混同しない。

入力seedや局面が複数候補なら、全候補について偽を示して初めて入力全体を偽にできる。
観測がターン途中で終わる場合も、未観測の行動を勝手に確定させない。
必要な境界条件や最初の行動の制約を、判定問題の入力に含める。

対象行動は次の8個。

```text
ATTACK_ALLY, DRAGON_SLASH, DEFENCE, FLEE_ALLY,
MEDICINAL_HERBS, HEAL, CRACK_ALLY, ACROBATIC_STAR
```

薬草、MP、必殺の合法性を検査し、明示した行動を1ターン実行する。
行動未指定時にHPから行動を選ぶ既存のフォールバックは使用しない。
FLEEは麻痺やinactiveの更新も変えるので、必ず含める。

敵HPが0の状態は成功終端 `G`、敵が生存し味方HPが0の状態は失敗終端とする。
成功を確認した経路は、それ以降のターンを必要としない。

**2. 元の2案から変える点**

Boolean行列の案は、完全状態を基底にすると全探索と同じ状態数を持つ。
`r[t+1]=A r[t]` と書くだけでは、d(13)付近の組み合わせ爆発は解消しない。

ホテル案も、部屋ごとに任意幅のPareto集合を持つ限り、人数の上限がない。
敵HPの少なさを優越とする削減は、怒り・敵の吸収回復・乱数消費の対応を失いうる。
1ターンの遷移署名の一致は、全将来の優越の証明にはならない。

ここでは、両案の次の部分を引き継ぐ。

- 固定seedの乱数テープと、その正確な位置 `p`。
- 未来の制御に関係する条件によるホテルの部屋分け。
- 疎な遷移作用素を、ターン数に沿って反復する構造。

変えるのは、作用素が運ぶ量である。
個体集合のORではなく、**撃破可能条件の上限をmax-plus演算で運ぶ**。

**3. 状態の値と、証明表の添字を分ける**

実状態は

\[
S=(p,c,E,A,M,I)
\]

と表す。`c` は、麻痺・inactive・必殺・アクロ・怒り・カメラ等の制御情報。
必要な実状態や再実行用の状態は、可逆な `uint64_t[3]` で保持してよい。
ハッシュ値を状態として扱わない。

一方、証明表の添字は

\[
q=\pi(S)=(p,\text{cell}(S))
\]

とする。`cell` は後述の述語分割の葉番号であり、代表個体ではない。
一つのセルに含まれる実状態は、すべて不等式の検査対象になる。

例として、カメラが同じで、麻痺中かどうかが同じ状態を同じセルにできる。
セルに残していない残りターン等については、可能な各値の遷移を上限へ含める。
同室の全員へ、一人の遷移結果を適用してはならない。

資源値について `E>=228` のような分岐条件をセルの区別に使う場合も、
`E` を228や帯の中央値へ置き換えない。`E` 自体は正確な整数変数のままである。
述語の真偽を記憶することと、ステータスの数値を量子化することは別の操作である。

`NowState` 全体などを保持する実状態と、証明表の添字から省ける情報も区別する。
例えば現在の強制行動・ターン境界モデルでは、ターン番号は残り予算と開始時点から与えられる。
前ターンの味方defenceは次ターン冒頭に必ず上書きされる。
このような省略は、現ソースで次の読み出しまでの挙動を確認したものだけ認める。

**4. 一つの辺は、一個体の実行結果ではなく、条件付きの更新式にする**

辺 `tau` に以下を持たせる。

```text
source cell / command / input guard
destination cell または G または失敗終端
E', A', M', I' の更新式
乱数位置・制御状態の更新と、その分岐の根拠
```

入力ガードの具体例は、

```text
A ∈ [18,65], E ∈ [228,244], M ∈ [2,22], I ∈ [0,Imax]
```

のような**全整数値の集合**である。
ガード内のHPを代表値へ丸める意味ではない。

固定 `p` と一つの制御分岐に沿えば、乱数の読み位置とダメージ値は決まる。
現在の8行動では、資源更新はガードを分割すると、各座標について

\[
x'=a_x x+b_x,\qquad a_x\in\{0,1\}
\]

の形になる。通常の加減算では `a=1`、HPの上限・下限へのclampでは `a=0`。
MPと薬草の消費は定数の減算である。

この性質は現在の対象コマンドと固定ステータスに対するもの。
将来、残MP比例ダメージなどが追加されたら、この更新形式に黙って押し込まない。
別の検証済み上限を用意するか、対応外とする。

現在のソースで必要な分割には、少なくとも以下がある。

| 読み出し・更新 | 保持すべき条件 |
| --- | --- |
| 生死判定、`process7A8` | `A>D`、`E>D` 等。死亡時の乱数消費の差も含む |
| 味方攻撃後の必殺判定 | `E-D>=0`。上の厳密不等号と混同しない |
| `ProcessRage` | 攻撃直前・直後の正確な敵HPと228・114の比較 |
| 味方の回復 | `A+R<=maxHp` と上限へのclamp |
| 敵のHP_HOOVER | `E+R<=enemy.maxHp` と敵HPのclamp |
| 行動の合法性 | MPが2以上・3以上、薬草が1以上など |
| 麻痺・inactive・アクロ・必殺・怒り | 実際のフラグ、残りターン、解除や発動の分岐 |
| カメラ | 実際のカウンタと、それによる乱数消費 |

怒りは「戦闘で一度だけ閾値を越える」と仮定しない。
HP_HOOVERで敵HPが回復すると、閾値を再び越える可能性がある。
敵の回復をダメージ0の行動として処理することも禁止する。

現ソースの物理ダメージ、呪文ダメージ、必殺率の計算は、現在HP・MP・薬草数そのものには依存しない。
`process7A8` のダメージ割合テーブルも現在HPではなく固定の `baseHP` から作られている。
このため、上記の資源ガードは、制御分岐を固定した後には一つの座標の整数区間へ分解できる。

**5. 辺を作る際の被覆義務**

代表HPで何回か実行して更新式を推測する方法では、証明にならない。
必要なのは、次の有限な被覆木である。

```text
入力セルの全領域
  資源・制御の比較を読む
    条件が全域で真 → 真側のみ
    条件が全域で偽 → 偽側のみ
    両方ある → 真になる領域と偽になる領域へ分割
  元の乱数・ダメージ計算を実行
  各葉に、そのガードと更新式を記録
```

例えば `E-D<228` は `E<=227+D` である。
現在の `E` の式が初期変数からの平行移動なら、それを代入して入力区間を切る。
clamp後に定数になっていれば、その比較は定数として判定する。

分割の両側の和集合が親領域であること、空領域だけを除いたことを記録する。
終端セルの分類にも未確定の述語があれば、その述語の逆像で入力領域を分割する。

実装担当者は、元のエミュレーターの乱数・ダメージ計算を利用し、
資源比較と更新に対する式の追跡を付加する形を選べる。
独立したダメージ式の移植や、量子化した戦闘エミュレーターを作る必要はない。
ただし、**この比較・更新の追跡が、本案の実装上もっとも重要な作業**である。
既存の `Step` を一回呼ぶだけで、全ガードを列挙できるわけではない。

固定seedのC++の乱数結果・整数ダメージを使う。
現在の `double` 計算を、勝手に `float` や別の整数ダメージ式へ置き換えない。
証明表と、その後の計算は64bit整数だけで扱える。

**6. 重みは優先スコアではなく、保存則の補正項である**

\[
\Phi(S)=u A+v M+w I,\quad Q>0,\quad u,v,w\ge0
\]

を定義する。全部整数とする。
辺 `tau` の重みは、入力ガード内での次の量の上限。

\[
\boxed{
W_\tau=
\max_{S\in\operatorname{guard}(\tau)}
\{Q(E-E')+\Phi(S')-\Phi(S)\}
}
\]

敵HPを減らすこと、味方のHPを失うこと、回復すること、MP・薬草を消費することを、
一つの**検査可能な不等式**へ変換している。
この値で個体や行動を落とすことはない。全合法行動について上限を取る。

HP_HOOVERなどで敵が回復した辺では `E-E'` は負になる。
味方HPの増分も、回復表示値ではなく実際のclamp後の増分を使う。

資源区間が直積で、更新式が前節の形なら、この最大化のためにHPの全値を走査する必要はない。
式の各座標の係数は、

\[
E:\ Q(1-a_E)\ge0,\quad
A:\ u(a_A-1)\le0,\quad
M:\ v(a_M-1)\le0,\quad
I:\ w(a_I-1)\le0.
\]

したがって、`E` は上端、`A,M,I` は下端を代入すればよい。
係数0の座標はどの値でも同じである。
これは単なる端点サンプリングではなく、区分アフィン式の最大値の計算である。

辺ごとに、この端点で得た

\[
(D_\tau,\Delta A_\tau,\Delta M_\tau,\Delta I_\tau)
\]

を保存すると、係数を変えても

\[
W_\tau=Q D_\tau+u\Delta A_\tau+v\Delta M_\tau+w\Delta I_\tau
\]

だけで重みを再計算できる。戦闘を再実行する必要はない。

座標間の条件が発生する拡張では、この端点公式をそのまま使えない。
その場合は領域全体への上限を検証する別の処理が必要である。

**7. Boolean行列を、証明用のmax-plus行列へ替える**

同じ始点・終点を持つ辺の重みの最大値を、

\[
\mathcal M_{q,q'}=\max_{\tau:q\to q'}W_\tau
\]

とする。存在しない辺は `-infinity`。
元資料と違い、ここでは行が始点、列が終点の記法にする。
実装は疎な辺の配列でよく、dense行列を作らない。

成功終端だけを0として初期化する。

\[
B_0(G)=0,\qquad B_0(q)=-\infty\quad(q\ne G).
\]

\[
\boxed{
B_h(G)=0,\qquad
B_h(q)=\max_{\tau:q\to q'}
\{W_\tau+B_{h-1}(q')\}.
}
\]

成功終端からは重み0で成功終端に留まれる、と考える。
これによって「ちょうどh」ではなく「h以内」の成功を扱う。
敵が生きたまま味方が死亡する辺は、成功終端へ接続しない。

`B0` を全セル0にしてはいけない。
それでは撃破へ至らない経路まで最大化し、上限を不必要に緩める。

**8. d(H)=偽を導く定理と証明**

仮定は二つ。

1. あらゆる合法な実遷移 `S→S'` が、対応するセル間の辺で被覆されている。
2. その辺の重みは `Q(E-E')+Phi(S')-Phi(S)` 以上である。

`S0` から `k<=H` ターンで撃破する実経路が存在すると仮定する。
その経路に対応する抽象辺を `tau1,...,tauk` とする。
各辺の上限を足すと、

\[
\begin{aligned}
\sum_{i=1}^{k}W_{\tau_i}
&\ge
\sum_{i=1}^{k}
\bigl[Q(E_{i-1}-E_i)+\Phi(S_i)-\Phi(S_{i-1})\bigr]\\
&=Q(E_0-E_k)+\Phi(S_k)-\Phi(S_0)\\
&=QE_0+\Phi(S_k)-\Phi(S_0)\\
&\ge QE_0-\Phi(S_0).
\end{aligned}
\]

最後は、撃破時にもHP・MP・薬草数と係数が非負であることを使った。
途中の資源はすべて相殺される。
そのため、13ターン目のHP・MP・薬草数の全組み合わせを保持する必要がない。

`B_H(q0)` はH以内に成功終端へ至るすべての抽象経路の重みの最大値なので、

\[
B_H(q_0)\ge\sum_i W_{\tau_i}\ge QE_0-\Phi(S_0).
\]

よって、

\[
\boxed{QE_0>B_H(q_0)+uA_0+vM_0+wI_0}
\]

なら、そのような勝利経路は存在しない。証明終わり。

`B_H(q0)=-infinity` の場合も、成功終端への抽象経路自体がなく、偽である。
逆向きは成立しない。不等式が成立しないことは、実戦で勝てる証明ではない。

**9. 係数がヒューリスティクスと異なることの小例**

以下は理解用の架空の戦闘であり、このプロジェクトの実測値ではない。

```text
攻撃：敵HPを20減らし、味方HPを15失う
回復：敵HPは変わらず、味方HPを最大25回復する
初期の味方HP=65、敵HP=300、予算=20ターン
```

資源を無視した上限は `20×20=400` で、倒せないとは言えない。

ここで `Q=2,u=1,v=w=0` とすると、

```text
攻撃の重み = 2×20-15 = 25
回復の重み <= 2×0+25 = 25
```

どんな20行動の組み合わせでも重みは500以下。
撃破するには定理から少なくとも `2×300-65=535` 必要なので、矛盾する。

攻撃を優先したわけでも、回復を不利として消したわけでもない。
**すべての行動を残したまま、資源の収支で上限を締めた**のである。
実際の案では、これに乱数位置ごとの攻撃機会、会心、状態異常、消費先の違いが入る。

