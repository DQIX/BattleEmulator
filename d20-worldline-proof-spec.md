**最短ターン数の証明仕様 — 近似による d(N−1)=偽 と、同じ規則で再実行する d(N)=true**

2026-09-05。これは設計と数学的な十分条件の証明であり、探索プログラムの実装ではない。

本改訂は、yo2_beの現行ソースと利用者が指定した前半系2例の戦闘ファイルを根拠にする。
PoCの戦闘の意味は、yo2_beの `BattleEmulator.cpp`、`Player.h`、`camera.cpp`、`lcg.cpp`、初期値の定義を基準とする。
ただし、利用者が明示した「眠り・麻痺中は逃げることができない」を優先する。
現コードとの不一致をそのまま正しい遷移として採用してはならない。詳細は第1節に定める。
横展開の調査は、指定されたbilyouma・zilyadamaのブランチ先端の `BattleEmulator.cpp` と `Player.h` に限る。
探索アルゴリズム、過去の研究レポート、Gitの過去の版は根拠にしない。
まずyo2_beのPoCで最短性の組と実用時間を確認する。前半系への拡張条件は第18節、
指定4ファイルからの具体的な根拠は補足仕様の第17・18節に置く。後半系への適用は対象外とする。

**提案の要点**

ホテルに泊めるものを、到達した個体から「この条件では撃破できないことを示す不等式」に変える。

乱数位置ごとにHP・MP・薬草の異なる個体を増やす代わりに、
残りターン数とホテルの部屋ごとに、原則として符号付き64bit整数を一つ保存する。
実際のHP・MP・薬草数を丸めることはない。
これらは不等式の変数として正確に扱う。

否定側で最後に確認する形は、次である。

\[
\boxed{
Q E_0>
B_H(q_0)+u A_0+v M_0+w I_0
\quad\Longrightarrow\quad d(H)=\mathrm{偽}
}
\]

ここで、`E` は敵HP、`A` は味方HP、`M` はMP、`I` は薬草数。
`q` は乱数位置と部屋、`Q>0`、`u,v,w>=0` は証明用の整数係数。
`B` の定義、計算、検査方法を以下で与える。
同じ問題の正確な再実行で長さNの勝利手順を得て、`H=N-1` の否定証明と組にする。
量子化した証明価格や外側近似は使えるが、TRUE側は同じ規則の実手順で確認する。

この方式では、ターン間で資源と制御状態の細かな対応を忘れる近似を使う。
その近似で生じる楽観的な可能性まで上限へ含める。
したがって、不等式を検査できた場合は、近似を使って実際の不可能性を証明できる。
経路のサンプリングだけから不可能性を推定する必要がない。

ただし、この上限が対象のN-1ターンで十分に小さくなること、全計算が数百ms〜15秒で終わることは未実測である。
本書が証明するのは「この条件が満たされたら偽である」と「計算対象の大きさを履歴数から切り離せること」。
特定局面について、その条件が既に満たされたとは主張しない。

`aaa.txt` の指摘を受け、実装上の難所を
[一ターンの記号実行と被覆の独立検査](d20-proof-kernel-spec.md) に具体化した。
第5・14節の「被覆を作る／検査する」は、この補足の命令順序と証明記録に従う。
PoCでは補足仕様の第2・13〜16節の固定Box、mode、外包の保存検査、補完のpc、累積予算を採用する。

[数学的証明](dN-mathematical-proof.md) は、この被覆検査から `d(N-1)=偽` と最短Nを導く。
価格量子化の誤差評価と、任意で辺上限を切り上げ量子化する場合の余裕損失 `H(b-1)` も示す。
後者はPoCの必須機能ではない。
本書の一般的な説明から、任意のInvariant関数や任意pcの補完を追加してはならない。
最も難しいのは、この生成・検査と、小さい分割で上限を締められるかであり、DPの実装ではない。

2026-09-06補足。Supportをrefine後のpartitionに対して再構築する手順と、
rule_idを変更不可の登録versionに結びつける手順を明記する。定理や近似の方向の変更ではない。

**1. 何を判定するか**

固定seed、観測済み入力を反映した正確なターン境界状態を `S0` とする。

\[
d(H\mid S_0)=
[\exists\text{合法な行動列 }a_1\ldots a_k,
\ k\le H:\ E(S_k)=0].
\]

`H` はその局面からの残りターン数である。
戦闘全体の現最良手順が `L` ターン、観測済みが `t0` ターンなら、
その一つ下を調べる残り予算は `H=L-1-t0`。
NをS0からの勝利手順の長さとすれば `H=N-1` である。
戦闘開始からのターン数と、途中局面から使う残りターン数を混同しない。
固定した観測条件下での結果を、未固定の前半手順まで含む最短性へ一般化しない。
同一のseed・S0・規則・観測条件で `d(N-1)=偽` と `d(N)=true` がそろったときだけ最短Nと返す。

規則の同一性は、人間向けの名前ではなく `rule_id=(registry_namespace, rule_version)` で管理する。
一つのrule_idには、固定RuleProgram、数値設定・native関数、対応profile、正確な再実行との対応を
一つの変更不可の登録内容として結びつける。いずれかを変えたら新しいrule_versionを登録する。
証明書から登録内容を上書きしたり、同じ表示名の別versionを組にしたりしない。
登録と照合の模擬コードは補足仕様第16節、現ソースに未定義のRuleProgramの型と具体例は同第3節に置く。

共通の問題は `Problem=(rule_id, seed, 正確なS0, 開始ターンt0, 観測・行動制約)` とする。
S0はp・NowState・無効タイマーを含む生の値で照合し、証明用に省略したmodeだけでは比較しない。
N・H・partition・価格は共通問題の外に置く。refineしただけで規則のversionを変える必要はない。

入力seedや局面が複数候補なら、全候補について偽を示して初めて入力全体を偽にできる。
観測がターン途中で終わる場合も、未観測の行動を勝手に確定させない。
必要な境界条件や最初の行動の制約を、判定問題の入力に含める。

yo2_beのPoCでの対象行動は次の8個。他の前半系では、合法行動集合を別のプロファイルとして登録する。

```text
ATTACK_ALLY, DRAGON_SLASH, DEFENCE, FLEE_ALLY,
MEDICINAL_HERBS, HEAL, CRACK_ALLY, ACROBATIC_STAR
```

薬草、MP、必殺の合法性を検査し、明示した行動を1ターン実行する。
この対象ではHEALはMP2以上、CRACKはMP3以上、薬草は所持数1以上。
ACROは必殺フラグが立ち、ターン境界で必殺残りターンが正、かつアクロ中でない場合とする。
必殺残り0では、次ターン冒頭の減算で必殺が失効する。
行動未指定時にHPから行動を選ぶ既存のフォールバックは使用しない。
PoCは眠りなしの入力に限定し、対象経路に眠り付与がないことを確認する。
眠りを持つ前半系の型と減算位置は、補足仕様の第17節に従って追加する。

**FLEEの合法性と意味は、以下を必須条件とする。**

- 眠り中、または麻痺中は、逃げることができない。
- 行動選択時にその状態なら、FLEEを合法行動集合へ入れない。
- 選択後、先に動いた敵によって眠り・麻痺になった場合も、味方の行動時点で逃走不能を反映する。
  この場合、合法に選択した一ターン全体を削除するのではなく、行動不能時の正しい遷移へ進める。
  逃走の移動効果を発生させたり、状態異常の残りターン更新・解除判定を迂回したりしない。
- 行動可能時のFLEEは、乱数位置を変えるための有効な選択肢として必ず含める。
  敵へ直接ダメージを与えないことを理由に削除しない。

ホテルの比喩では、FLEEは**部屋と客をそのまま別のホテルへ引っ越す選択**である。
味方が攻撃や回復に資源を使う代わりに、未来に読む乱数位置を変える。
次の会心・敵行動・状態異常・解除判定への接続先を変えること自体に意味がある。

ただし、「そのまま」は、一ターン全体の敵行動やカウンタ更新まで凍結する意味ではない。
一ターンの遷移は

\[
(p,c,E,A,M,I)
\xrightarrow{\mathrm{FLEE}}
(p+\Delta p_{\mathrm{FLEE}}(S),c',E',A',M',I')
\]

であり、敵行動、ターン経過、カメラによって実際に変わる値は反映する。
味方の逃走選択自体が変えない資源・制御情報は保持する。
移動先の乱数位置を任意に選べることにも、無料・0ターンの移動にもしてはならない。

本証明のDPでは、FLEEも1ターンを使う通常の辺である。
仮に資源の増減がなく辺の重みが0でも、行き先 `q'` が違えば `B[h-1][q']` は変わる。
したがって重み0を「無駄な辺」として消すことも禁止する。

**現コードとの不一致。** 現在の `Main` は `FLEE_ALLY` で先に `skipTurn=true` とし、
その後の麻痺・inactive処理を迂回する。眠り中の逃走禁止も、この箇所では検査していない。
従って、現在の `Main` を無条件に正本として呼ぶだけでは、上記の逃走規則を満たさない。
実装担当者は、合法性と行動実行時の状態判定を仕様に合わせた遷移で被覆を作り、再実行確認も同じ規則で行う。
入口の合法性検査だけを追加して、敵が先に状態異常を与える場合を取り残してはならない。
本書では仕様上の差分だけを記録し、エミュレーターの実装は変更しない。

敵HPが0の状態は成功終端 `G`、敵が生存し味方HPが0の状態は失敗終端とする。
成功を確認した経路は、それ以降のターンを必要としない。

**2. 元の2案から変える点**

Boolean行列の案は、完全状態を基底にすると全探索と同じ状態数を持つ。
`r[t+1]=A r[t]` と書くだけでは、d(13)付近の組み合わせ爆発は解消しない。

ホテル案も、部屋ごとに任意幅のPareto集合を持つ限り、人数の上限がない。
敵HPの少なさを優越とする削減は、怒り・敵の吸収回復・乱数消費の対応を失いうる。
1ターンの遷移署名の一致は、全将来の優越の証明にはならない。

ここでは、両案の次の部分を引き継ぐ。

- 固定seedの乱数テープと、その正確な位置 `p`。
- 未来の制御に関係する条件によるホテルの部屋分け。
- 疎な遷移作用素を、ターン数に沿って反復する構造。

変えるのは、作用素が運ぶ量である。
個体集合のORではなく、**撃破可能条件の上限をmax-plus演算で運ぶ**。

**3. 状態の値と、証明表の添字を分ける**

実状態は

\[
S=(p,c,E,A,M,I)
\]

と表す。`c` は、眠り・麻痺・inactive・必殺・アクロ・怒り・カメラ等の制御情報と必要な残りターン。
PoCで必要な実状態や再実行用の状態は、可逆な `uint64_t[3]` で保持してよい。
保存した実状態から、元の各値を完全に復元できることを要求する。
証明用のmode化で無効タイマー等を省く写像は、この可逆保存とは別である。
横展開で資源や状態を追加する場合、実状態の保存容量も再検証する。

一方、証明表の添字は

\[
q=\pi(S)=(p,\text{cell}(S))
\]

とする。`cell` は後述の述語分割の葉番号であり、代表個体ではない。
一つのセルに含まれる実状態は、すべて不等式の検査対象になる。

例として、カメラが同じで、麻痺中かどうかが同じ状態を同じセルにできる。
セルに残していない残りターン等については、可能な各値の遷移を上限へ含める。
同室の全員へ、一人の遷移結果を適用してはならない。

資源値について `E>=228` のような分岐条件をセルの区別に使う場合も、
`E` を228や帯の中央値へ置き換えない。`E` 自体は正確な整数変数のままである。
述語の真偽を記憶することと、ステータスの数値を量子化することは別の操作である。

`NowState` 全体などを保持する実状態と、証明表の添字から省ける情報も区別する。
例えば現在の強制行動・ターン境界モデルでは、ターン番号は残り予算と開始時点から与えられる。
規則がその番号を読む場合は、層から復元した値を必ず渡す。
zilyadamaの敵行動回数は戦闘ターンの奇偶で変わるので、異なる奇偶で局所証明を無条件に共用できない。
前ターンの味方defenceは次ターン冒頭に必ず上書きされる。
このような省略は、現ソースで次の読み出しまでの挙動を確認したものだけ認める。

**4. 一つの辺は、一個体の実行結果ではなく、条件付きの更新式にする**

辺 `tau` に以下を持たせる。

```text
source cell / command / input guard
destination cell または G または失敗終端
E', A', M', I' の更新式
乱数位置・制御状態の更新と、その分岐の根拠
```

入力ガードの具体例は、

```text
A ∈ [18,65], E ∈ [228,244], M ∈ [2,22], I ∈ [0,Imax]
```

のような**全整数値の集合**である。
ガード内のHPを代表値へ丸める意味ではない。

固定 `p` と一つの制御分岐に沿えば、乱数の読み位置とダメージ値は決まる。
現在の8行動では、資源更新はガードを分割すると、各座標について

\[
x'=a_x x+b_x,\qquad a_x\in\{0,1\}
\]

の形になる。通常の加減算では `a=1`、HPの上限・下限へのclampでは `a=0`。
MPと薬草の消費は定数の減算である。

この性質は現在の対象コマンドと固定ステータスに対するもの。
将来、残MP比例ダメージなどが追加されたら、この更新形式に黙って押し込まない。
別の検証済み上限を用意するか、対応外とする。

現在のソースで必要な分割には、少なくとも以下がある。

| 読み出し・更新 | 保持すべき条件 |
| --- | --- |
| 生死判定、`process7A8` | `A>D`、`E>D` 等。死亡時の乱数消費の差も含む |
| 味方攻撃後の必殺判定 | `E-D>=0`。上の厳密不等号と混同しない |
| `ProcessRage` | 攻撃直前・直後の正確な敵HPと228・114の比較 |
| 味方の回復 | `A+R<=maxHp` と上限へのclamp |
| 敵のHP_HOOVER | `E+R<=enemy.maxHp` と敵HPのclamp |
| 行動の合法性 | MPが2以上・3以上、薬草が1以上、眠り・麻痺中のFLEE禁止など |
| 眠り・麻痺・inactive・アクロ・必殺・怒り | 実際のフラグ、残りターン、解除や発動、それぞれの乱数消費 |
| カメラ | 実際のカウンタと、それによる乱数消費 |

**敵HPの3群と閾値通過の消費を明示する。** 敵最大HP456では、生存中の区分は次のとおり。

| 群 | 正確な敵HP |
| --- | --- |
| 50%以上 | 228〜456 |
| 25%以上・50%未満 | 114〜227 |
| 25%未満 | 1〜113 |

敵HP0は成功終端。現ソースの厳密な不等号に従い、228と114はそれぞれ上側の群に属する。
各群に一個の固定消費量を割り当てるのではなく、攻撃の直前・直後のHPから閾値通過を判定する。
同じ50%以上の群でも、230から10減らす場合と300から10減らす場合は別の消費になる。

現在の `ProcessRage` 一回分の追加乱数消費は、以下のとおり。
表の数は攻撃全体の消費量ではなく、この関数による追加分だけである。

| 直前→直後の条件 | 怒りなし | 怒り中 |
| --- | --- | --- |
| 228以上→227以下 | 2消費。怒りを発動し、継続2〜4ターンを抽選 | 1消費 |
| 114〜227→113以下 | 2消費。この分岐自体では新たな怒りを発動しない | 1消費 |
| 上のどちらでもない | 0消費 | 0消費 |

一回の呼び出しで両閾値をまたぐ場合も、現ソースでは50%側の分岐を処理し、25%側を重ねて処理しない。
カウンターと味方の攻撃で一ターンに複数回呼ばれる場合は、各呼び出し時点のHPでそれぞれ判定する。
怒り中は敵の攻撃先決定などの消費も変わるので、この表の追加分だけで一ターンの消費を決めない。

**アクロ・眠り・麻痺も、乱数消費の異なる遷移として扱う。**
アクロは回避・カウンター・通常被弾の分岐と、その中の消費まで含む。
麻痺は残りターンの減算、解除抽選、行動不能による消費差まで含む。
眠りも行動不能、継続・解除、必要なら被弾による復帰の規則と消費を区別する。
いずれも「状態があるので攻撃ダメージだけ0にする」という置き換えでは不十分である。

眠りについて、現在のファイルには `sleeping` と `sleepingTurn` はあるが、
今回読んだ8行動用の `Main` には、眠りの継続・解除を完結させる処理が確認できない。
その具体的な解除確率や消費数を、麻痺と同じだと推測して埋めない。
眠りが到達可能な対象を扱う場合は、その正式な遷移を被覆へ含める。
未定義部分は検証済みの補完上限で覆うか未判定とし、通常行動へ置換して偽を出さない。
対象局面から眠りに到達しないことを確認できる場合だけ、その不変条件を使って眠り分岐を省ける。
`sleeping` と `inactive` も、名称だけで同じ状態とみなさない。

怒りは「戦闘で一度だけ閾値を越える」と仮定しない。
HP_HOOVERで敵HPが回復すると、閾値を再び越える可能性がある。
敵の回復をダメージ0の行動として処理することも禁止する。

現ソースの物理ダメージ、呪文ダメージ、必殺率の計算は、現在HP・MP・薬草数そのものには依存しない。
`process7A8` のダメージ割合テーブルも現在HPではなく固定の `baseHP` から作られている。
このため、上記の資源ガードは、制御分岐を固定した後には一つの座標の整数区間へ分解できる。

**5. 辺を作る際の被覆義務**

代表HPで何回か実行して更新式を推測する方法では、証明にならない。
必要なのは、次の有限な被覆木である。

木の根は「kernelが作る経過ターンtの外包Envelope_t、入力セル、合法な選択領域の共通部分」とする。
FLEEなら、選択時の眠り・麻痺を除いた領域から始め、実行時の状態変化は木の内部で再判定する。

```text
入力セルの全領域
  資源・制御の比較を読む
    条件が全域で真 → 真側のみ
    条件が全域で偽 → 偽側のみ
    両方ある → 真になる領域と偽になる領域へ分割
  元の乱数・ダメージ計算を実行
  各葉に、そのガードと更新式を記録
```

例えば `E-D<228` は `E<=227+D` である。
現在の `E` の式が初期変数からの平行移動なら、それを代入して入力区間を切る。
clamp後に定数になっていれば、その比較は定数として判定する。

分割の両側の和集合が親領域であること、空領域だけを除いたことを記録する。
終端セルの分類にも未確定の述語があれば、その述語の逆像で入力領域を分割する。

実装担当者は、元のエミュレーターの乱数・ダメージ計算を利用し、
資源比較と更新に対する式の追跡を付加する形を選べる。
独立したダメージ式の移植や、量子化した戦闘エミュレーターを作る必要はない。
ただし、**この比較・更新の追跡が、本案の実装上もっとも重要な作業**である。
既存の `Step` を一回呼ぶだけで、全ガードを列挙できるわけではない。

具体的には、四つの資源式と整数区間を扱う小さい記号実行器が必要である。
被覆木には規則上のpc、比較、子領域、乱数読み、更新、終了先を記録する。
検査器は固定された一ターンの規則から次の命令と領域を計算し、
非空の枝が欠けた場合も、本来必要な比較自体が飛ばされた場合も拒否する。
根の合法入力領域も、生成器の主張からではなく検査器側で作る。
詳細な型、検査手続き、信用する規則の範囲は補足仕様の第1〜6節に定める。

入力領域は、補足仕様第14節でkernelが構築・検査する固定形式の外包にする。
PoCの継続資源は `1<=A<=65`、`1<=E<=456`、`0<=M<=M0`、`0<=I<=I0`。
制御は、次の有効値と無効値を区別した六つの固定enumのmaskとする。
任意の不変条件を生成器に探索させることを前提にしない。

| 制御値 | 有効中の保守的な範囲 |
| --- | --- |
| 味方必殺の残りターン | 0〜6 |
| 眠り | yo2_beのPoCでは到達しない。眠りを含む入力は別プロファイル |
| 麻痺の残りターン | -2〜4。次の-3では解除確率が1になり解除される |
| アクロの残りターン | 1〜6 |
| 敵怒りの残りターン | 1〜4 |
| カメラカウンタ | 0〜5 |

Inactiveの真偽も制御に含める。麻痺レベルの増加は、現在値と残り予算から整数安全性を検査する。
無効中のタイマーは、実状態では正確な値を保持する。
セルで同一視する場合は、再び有効になるまでの読み出しが挙動へ影響せず、
有効化時に上書きされることを確認する。
kernelはS0が外包に入ること、全詳細葉・全補完の継続出力が次層の外包に入ること、
成功・失敗終端の資源が非負であることを検査する。出力を外包で切り落としてから包含を主張してはならない。
必要な根は、検査済みの全出力先からkernelが前向きに作る支持集合で決める。
途中状態がこのプロファイルと異なれば、値を切り詰めず未対応にする。

固定seedのC++の乱数結果・整数ダメージを使う。
現在の `double` 計算を、勝手に `float` や別の整数ダメージ式へ置き換えない。
証明表と、その後の計算は64bit整数だけで扱える。

**6. 重みは優先スコアではなく、保存則の補正項である**

\[
\Phi(S)=u A+v M+w I,\quad Q>0,\quad u,v,w\ge0
\]

を定義する。全部整数とする。
辺 `tau` の重みは、入力ガード内での次の量の上限。

\[
\boxed{
W_\tau=
\max_{S\in\operatorname{guard}(\tau)}
\{Q(E-E')+\Phi(S')-\Phi(S)\}
}
\]

敵HPを減らすこと、味方のHPを失うこと、回復すること、MP・薬草を消費することを、
一つの**検査可能な不等式**へ変換している。
この値で個体や行動を落とすことはない。全合法行動について上限を取る。

HP_HOOVERなどで敵が回復した辺では `E-E'` は負になる。
味方HPの増分も、回復表示値ではなく実際のclamp後の増分を使う。

資源区間が直積で、更新式が前節の形なら、この最大化のためにHPの全値を走査する必要はない。
式の各座標の係数は、

\[
E:\ Q(1-a_E)\ge0,\quad
A:\ u(a_A-1)\le0,\quad
M:\ v(a_M-1)\le0,\quad
I:\ w(a_I-1)\le0.
\]

したがって、`E` は上端、`A,M,I` は下端を代入すればよい。
係数0の座標はどの値でも同じである。
これは単なる端点サンプリングではなく、区分アフィン式の最大値の計算である。

辺ごとに、この端点で得た

\[
(D_\tau,\Delta A_\tau,\Delta M_\tau,\Delta I_\tau)
\]

を保存すると、係数を変えても

\[
W_\tau=Q D_\tau+u\Delta A_\tau+v\Delta M_\tau+w\Delta I_\tau
\]

だけで重みを再計算できる。戦闘を再実行する必要はない。

座標間の条件が発生する拡張では、この端点公式をそのまま使えない。
その場合は領域全体への上限を検証する別の処理が必要である。

**7. Boolean行列を、証明用のmax-plus行列へ替える**

同じ始点・終点を持つ辺の重みの最大値を、

\[
\mathcal M_{q,q'}=\max_{\tau:q\to q'}W_\tau
\]

とする。存在しない辺は `-infinity`。
ここでは行が始点、列が終点の記法にする。
実装は疎な辺の配列でよく、dense行列を作らない。

成功終端だけを0として初期化する。

\[
B_0(G)=0,\qquad B_0(q)=-\infty\quad(q\ne G).
\]

\[
\boxed{
B_h(G)=0,\qquad
B_h(q)=\max_{\tau:q\to q'}
\{W_\tau+B_{h-1}(q')\}.
}
\]

成功終端からは重み0で成功終端に留まれる、と考える。
これによって「ちょうどh」ではなく「h以内」の成功を扱う。
敵が生きたまま味方が死亡する辺は、成功終端へ接続しない。

ターン番号で規則が変わるプロファイルでは、経過tの辺集合を `E_t` として同じ計算を行う。
固定したHに対する層hでは、`t=H-h` の辺を使う。
開始ターンと層から決まる行動回数を保持すれば、ターン番号を自由に選べる抽象遷移にはならない。
この場合も、H層の各辺が対応する実遷移を覆うことが第8節の証明の条件である。

`B0` を全セル0にしてはいけない。
それでは撃破へ至らない経路まで最大化し、上限を不必要に緩める。

**8. d(H)=偽を導く定理と証明**

仮定は二つ。

1. あらゆる合法な実遷移 `S→S'` が、対応するセル間の辺で被覆されている。
2. その辺の重みは `Q(E-E')+Phi(S')-Phi(S)` 以上である。

`S0` から `k<=H` ターンで撃破する実経路が存在すると仮定する。
その経路に対応する抽象辺を `tau1,...,tauk` とする。
各辺の上限を足すと、

\[
\begin{aligned}
\sum_{i=1}^{k}W_{\tau_i}
&\ge
\sum_{i=1}^{k}
\bigl[Q(E_{i-1}-E_i)+\Phi(S_i)-\Phi(S_{i-1})\bigr]\\
&=Q(E_0-E_k)+\Phi(S_k)-\Phi(S_0)\\
&=QE_0+\Phi(S_k)-\Phi(S_0)\\
&\ge QE_0-\Phi(S_0).
\end{aligned}
\]

最後は、撃破時にもHP・MP・薬草数と係数が非負であることを使った。
途中の資源はすべて相殺される。
そのため、13ターン目のHP・MP・薬草数の全組み合わせを保持する必要がない。

`B_H(q0)` はH以内に成功終端へ至るすべての抽象経路の重みの最大値なので、

\[
B_H(q_0)\ge\sum_i W_{\tau_i}\ge QE_0-\Phi(S_0).
\]

よって、

\[
\boxed{QE_0>B_H(q_0)+uA_0+vM_0+wI_0}
\]

なら、そのような勝利経路は存在しない。証明終わり。

`B_H(q0)=-infinity` の場合も、成功終端への抽象経路自体がなく、偽である。
逆向きは成立しない。不等式が成立しないことは、実戦で勝てる証明ではない。

**9. 係数がヒューリスティクスと異なることの小例**

以下は理解用の架空の戦闘であり、このプロジェクトの実測値ではない。

```text
攻撃：敵HPを20減らし、味方HPを15失う
回復：敵HPは変わらず、味方HPを最大25回復する
初期の味方HP=65、敵HP=300、予算=20ターン
```

資源を無視した上限は `20×20=400` で、倒せないとは言えない。

ここで `Q=2,u=1,v=w=0` とすると、

```text
攻撃の重み = 2×20-15 = 25
回復の重み <= 2×0+25 = 25
```

どんな20行動の組み合わせでも重みは500以下。
撃破するには定理から少なくとも `2×300-65=535` 必要なので、矛盾する。

攻撃を優先したわけでも、回復を不利として消したわけでもない。
**すべての行動を残したまま、資源の収支で上限を締めた**のである。
実際の案では、これに乱数位置ごとの攻撃機会、会心、状態異常、消費先の違いが入る。

**10. DP表を有限にし、局所証明の処理量も別に制限する**

資源の全組み合わせを除いただけでは、制御状態の直積が膨らむ可能性が残る。
これも無制限に展開してはならない。

ホテルの部屋を、各乱数位置につき最大 `K` 枚の述語セルと定義する。
述語分割は二分木にし、葉同士は交わらず、対象領域全体を覆うものにする。
初期にはカメラの値を区別する程度から始め、必要な分岐条件だけを追加できる。
すべての状態フラグと残りターンを最初から直積にしない。

この上限は「K人残す」という上限ではない。
一つのセルの不等式が、何億個の実状態へ同時に適用されても、表の要素は一つである。
部屋に入りきらない個体という概念がない。

使用する乱数位置が `P` 個なら、証明表のセル数は必ず

\[
n_{\rm cell}\le P K
\]

となる。保持する制御条件を増やすときも、葉数 `K` を超えて分割しない。
分割できない条件は、引き続きその全場合を辺の上限へ含める。

一方、一つの行動の被覆木の葉数も無制限にはしない。
DPへ渡す詳細な辺の項数を最大 `J`、未展開部分を覆う**保守的な補完の項数**を最大 `C` とする。
補完を複数の一次式の最大値で表す場合は、その一次式の数も `C` に数える。
未展開の枝を削除する操作ではない。

補完辺は、未展開部分で起こりうる更新と行き先を全部含む上限である。
例えば現在の8行動・固定能力値では、粗く

\[
E-E'\le D_{\max},\quad A'-A\le A_{\max},\quad
M'-M\le0,\quad I'-I\le0
\]

と置けるので、

\[
W_{\rm top}=QD_{\max}+uA_{\max}
\]

を使える。未確定の行き先集合を `Targets` とすると、この補完辺の寄与は

\[
W_{\rm top}+\max_{q'\in\mathrm{Targets}}B_{h-1}(q')
\]

である。成功終端も可能なら `Targets` に含め、その値0とも最大を取る。

最も粗い `Dmax` は敵の最大HPでよい。
現能力値では、味方の自分の行動の与ダメージは高々64、
敵の1行動によるアクロのカウンターも高々64なので、`Dmax=128` まで締められる。
通常攻撃以外の対象攻撃はこの上限以下で、敵の回復は正味の減少を増やさない。
この128は**与ダメージの上限**であり、乱数の最大消費数とは別の定数である。
能力値や行動集合を変えた場合は、この小さい上限を再検証する。

行き先集合は範囲として表現してよい。
全セルを対象にする場合の最大値は層ごとに一回計算すればよく、
補完辺を `n_cell` 本の実辺へ展開する必要はない。
乱数位置の進行範囲まで検証できれば、対象の位置範囲に限定した最大値を使える。

この補完はかなり楽観的なので、多用すると証明できなくなる。
しかし、サイズ制限へ到達したときに証明の意味を変えずに計算を継続できる。
締める価値があるのは、最大値を決めている経路上の補完部分である。

実用時には、補足仕様の第8・9節の行動別・実行位置別の上限を使う。
回復しない行動へ無料のHP回復を与えず、回復とMP・薬草消費を同じ場合として保持する。
途中まで実行した後は、支払い済みの資源や確定したダメージを補完で取り消さない。

以上から、詳細辺と補完辺の記録数は

\[
|\mathcal E_{\rm terms}|\le8PK(J+C).
\]

係数候補を `R` 回評価する計算量は、局所被覆の生成費を別にして、

\[
\boxed{O\bigl(RH\,8PK(J+C)\bigr)}.
\]

`8^13`、到達個体数、Pareto幅はこの式に入らない。
13層目も12層目と同じ大きさの表を更新する。
ただし、これはDPの保存量と辺評価数の上限である。
局所記号実行、領域の制限、接続の更新、証明ノードの検査の費用は別に必要になる。
それらの総処理量 `V` と、証明記録の総byte数にも上限を置く。
DP用の辺数が小さいことから、生成と検査まで軽いとは推論しない。

ただし、サイズを制限した上限で不等式が成立する保証はない。
**組み合わせ爆発を防ぐ保証と、15秒以内に最短性を確定できる保証は異なる。**
後者の成否は、対象局面でどこまで上限を締める必要があるかに依存する。

**11. 近似の場所と、細かくすると何が改善するか**

ある抽象経路では、前の辺が味方HP20で終わったのに、
次の辺が味方HP60を前提としているかもしれない。
敵HPについても、前後の怒り判定の条件が食い違う場合がある。
セルがこうした対応を記憶しなければ、そのような接続が残る。

本案は、この余分な接続を最初から完全に消そうとしない。
資源の増減を価格で計上して締め、必要な接続条件だけをセルに追加する。
一つの価格で十分に表せない、中間での死亡や怒りの発動時点の制約は、セル分割の役割になる。

分割に使うのは、上限を実現している経路と実際の戦闘の不一致である。

```text
抽象最大経路を取り出す
  行動列をS0から正本エミュレーターで再実行する
    本当にH以内に勝つ → TRUE。行動列を返す
    抽象辺のガードと食い違う → 最初の食い違いの条件を調べる
```

例えば、次の辺が `E<=227` を必要としているのに、実際には `E=239` だった場合、
その乱数位置のセルに `E<=227` という述語を追加する。
そのセルへ入る辺も、検査済みの更新式に対する出力述語の逆像で接続し直す。
ラベルだけを付け替えたり、その抽象辺を全体から削除したりしてはならない。

詳細なガードと更新式はセル番号・価格から分離して保持する。
セルを細分化しただけなら、既存の証明領域を制限して再利用できる。
未展開の補完部分の詳細が必要になった場合だけ、そのpcから記号実行を再開する。
セルを分けるたびに一ターン全体を生成し直す必要はない。再利用の条件は補足仕様の第7節に定める。

ただし、Supportは新partitionの初期セルから全層を再構築する。古いセル番号の支持をコピーしない。
検査済みの全継続出力先と、そこから必要になる全合法行動の根を確定してから、新しい辺でDPを計算する。
補完の詳細化で行き先が変わる場合も同様である。局所更新式の再利用と、支持・接続の再構築を混同しない。

直近の条件だけでは足りないときは、数ターン分の更新式に沿って条件を逆向きに引く。
例えば次に必要な条件が `E'<=227`、直前の式が `E'=E-16` なら、
その前の条件は `E<=243`。
これをさらに前へ引けば、必要な将来の閾値だけを覚えるセルを作れる。
clampでは、その条件の逆像が空・全域・区間のどれになるかを正確に計算する。

これは、全行動のkターン先の署名をすべて計算する方法ではない。
**現在の不可能性証明を邪魔している接続に必要な述語だけを逆算する方法**である。
どの実行経路も、分割前後で被覆されたままである。

`K` や計算予算を超える場合は、それ以上分割しない。
価格の再調整で締まるかを調べ、締まらなければ未判定とする。
偽を出すために不都合な辺を消すことはしない。

**12. 証明係数をどう決めるか**

固定した抽象グラフに対して、正規化した係数

\[
\theta=(u/Q,v/Q,w/Q)\ge0
\]

を使う。
最大経路を構成する各辺には、前述の資源増減ベクトルがある。
したがって、初期資源の項を加えた上限は

\[
U(\theta)=\max_{\text{H以内の抽象成功経路 }P}
\left\{
\sum_{\tau\in P}D_\tau+
\theta\cdot\left[(A_0,M_0,I_0)+\sum_{\tau\in P}\Delta R_\tau\right]
\right\}.
\]

これは3変数の区分線形な凸関数である。
経路数を列挙する必要はなく、max-plus DPが最大値と、その値を与える一本の経路を返す。
補完辺も、固定した上限ベクトルと行き先の最大値を使う限り、同じ形で扱える。

価格候補を作る方法として、以下の切除平面法を使える。
PoCでは `Q=256`、`0<=u,v,w<=2^20` 等の量子化した係数を候補にして、整数DPで最終確認する。
価格候補の生成自体は非信用部分なので、浮動小数を使ってもよい。
全量子化格子の探索や、最適価格の証明は要求しない。一組の十分条件で足りる。

```text
theta=0 でDPを一回実行
最大経路Pの一次式 fP(theta) を記録

反復：
  記録済みの全Pについて z >= fP(theta) を課し、zを最小化
  制約は theta>=0 と、設定した有限の係数上限も含む
  得られたthetaでDPを実行
  U(theta)<E0 なら、整数係数へ直して証明を検査
  そうでなければ、新しい最大経路の一次式を追加
```

小さい最適化問題は `theta` の3変数と `z` の計4変数だけである。
例えば候補平面を16本までに制限すれば、境界平面4枚の交点の列挙でも解ける。
有限の係数上限は、価格の探索範囲の制限であり、戦闘の数値の量子化ではない。

実装を64bit整数へ限定するなら、交点は有理数で扱い、共通分母を `Q` とする。
候補の係数を小さい分母の有理数へ変更してもよいが、変更後にはDPを必ず再計算する。
採用する不等式の検査は常に整数の厳密比較で行う。
計算中の積・和・有理数比較の中間値も、すべてオーバーフローを検査する。

既にある平面の最大値は `U` の下界なので、その最小値が `E0` 以上だと厳密に検査できれば、
設定した係数範囲内ではこの抽象化とこの価格の族で証明できないことが分かる。
この場合、同じ表で価格を延々試すのではなく、前節の分割・補完の改善へ進む。
浮動小数のLPの表示だけを、この下界の証明に使わない。
厳密な有理数LPは拡張として残し、PoCの必須部分にはしない。

この族が不十分でも、戦闘に勝利解があるとは限らない。
資源制約付き経路には、価格だけでは埋まらない緩和の差がありうる。
その差を無視して「最適な重みを見つければ必ず証明できる」と主張しない。

**13. 実装担当者へ渡す擬似コード**

```text
decide_bound(ctx, H, limits, shared_deadline, checked_cache):
    (S0, seed) = exact_input_from(ctx.Problem)
    validate_exact_input_and_supported_rules(ctx, H)
    if enemyHP(S0)==0: return kernel_check_initial_success(ctx, H)
    if heroHP(S0)==0 or H==0: return kernel_check_trivial_false(ctx, H)

    # 入力・勝利手順取得からの共通deadlineを使い、ここで15秒を取り直さない。
    tape = registered_rng_tape(ctx.bundle, seed)
    partition = initial_predicate_partition(max_cells_per_position=K)
    envelope = kernel_construct_envelopes(ctx, H)

    proof_provider = guard_certificate_provider(
        ctx, checked_cache, shared_deadline,
        max_detailed_cases_per_action=J,
        max_completion_terms_per_action=C,
        total_proof_work_limit=V,
        total_proof_bytes_limit=limits.proof_bytes,
        complete_unexpanded_cases_with_upper_edges=true)
    (Support, checked_coverage, model) = rebuild_support(
        ctx, H, partition, envelope, proof_provider, checked_cache)

    repeat within the shared time / work budgets:
        theta = next_bounded_coefficient_candidate()
        (Q,u,v,w) = checked_nonnegative_integer_coefficients(theta)

        B = new_empty_layer_tables(Support)
        B[0][Support[H]] = NEGATIVE_INFINITY
        B[0][GOAL] = 0

        for h in 1..H:
            B[h][GOAL] = 0
            prepare_maxima_for_completion_edges(B[h-1])

            for q in Support[H-h]:
                best = NEGATIVE_INFINITY
                for covered edge tau in model.edges_at_elapsed_turn(q, H-h):
                    if tau leads only to failure: continue
                    continuation = B[h-1][tau.target]
                    # completion edge: maximum over its whole target set
                    if continuation==NEGATIVE_INFINITY: continue
                    value = checked_add(weight(tau,Q,u,v,w), continuation)
                    best = max(best, value)
                B[h][q] = best

        root = B[H][project(S0, partition)]
        if root==NEGATIVE_INFINITY:
            candidate_is_false = true
        else:
            bound = checked_sum(root, u*A0, v*M0, w*I0)
            candidate_is_false = checked_product(Q,E0)>bound
        # Every multiplication inside checked_sum is checked too.
        # An undefined/unbounded upper value cannot establish candidate_is_false.
        if candidate_is_false:
            certificate = (ctx.Problem, H, partition, checked_coverage.proof_records, Q,u,v,w,B)
            if independently_verify_local_inequalities(ctx, certificate):
                return FALSE_WITH_CERTIFICATE(ctx.problem_key, H, certificate)
            return MODEL_OR_ARITHMETIC_ERROR

        P = a_maximizing_abstract_goal_path()
        replay = exact_replay(ctx, commands(P), shared_deadline)
        if replay is checked winning within H:
            return TRUE_WITH_REPLAY(ctx.problem_key, replay)

        if selected_unexpanded_completion_on(P) and proof_work_budget_remains:
            resume_and_verify_selected_checkpoint(ctx, P, proof_provider, checked_cache)
            (Support, checked_coverage, model) = rebuild_support(
                ctx, H, partition, envelope, proof_provider, checked_cache)
            restart_coefficient_LP_for_changed_graph()
            continue

        add_affine_constraint_from(P)
        if refinement_is_selected_or_exact_price_lower_bound_is_insufficient:
            partition = refine_a_separating_predicate_from(P, partition)
            (Support, checked_coverage, model) = rebuild_support(
                ctx, H, partition, envelope, proof_provider, checked_cache)
            restart_coefficient_LP_for_changed_graph()
            # Never reuse old LP lower bounds as bounds for a refined graph.

    return UNKNOWN_WITH_BOUND_AND_BUDGET_REPORT
```

`rebuild_support` は補足仕様第14節の手順である。毎回全層のSupportを空にして、
S0の属する新partitionの葉から、全詳細葉・補完の継続出力先と必要根を前向きに構築する。
`proof_provider` は要求された根の証明を供給する生成側の窓口で、必要根や空判定を決めるAPIではない。
適合する検査済みガードの領域制限は使えるが、不足する根は生成・検査するか、許可された補完で覆う。
最終検査では提出証明書から必要根を読み直し、欠落は拒否する。出力をEnvelopeで削る前の包含検査も必須である。
Support・checked_coverage・modelは再構築完了後に同時に差し替え、旧DPと補完用の最大表は破棄して再計算する。
必要な行き先が次層のSupportやDP表にない場合は構築エラーであり、存在しない辺や負の無限大として扱わない。
この再構築にも共通deadline・累積V・byte予算を使い、打切り時はUNKNOWNとする。
自明な結果にもctx.problem_key・対象H・検査記録を持たせ、通常の検査済み結果と同じ形式で返す。

上の手続きは一つのHを調べる。最短性を返す外側の手続きは次とする。

```text
prove_minimum(Problem, candidate_commands, limits):
    deadline = now + limits.total_time  # PoCでは最大15秒。入力受理から計時する。
    ctx = bind_problem(Problem)         # 補足仕様第16節。不変な登録内容を束縛する。
    (S0, seed) = exact_input_from(ctx.Problem)
    checked_cache = new_empty_kernel_cache(ctx.problem_key)
    if enemyHP(S0)==0: return OPTIMAL(0)

    witness = obtain_and_exactly_replay_candidate(ctx, candidate_commands, deadline)
    if no checked winning witness: return UNKNOWN_WITH_AVAILABLE_BOUNDS
    N = first_winning_turn(witness)

    repeat within deadline:
        result = decide_bound(ctx, N-1, limits, deadline, checked_cache)
        if result is checked FALSE:
            assert witness.problem_key == result.problem_key == ctx.problem_key
            assert result.H == first_winning_turn(witness)-1
            return OPTIMAL(N, witness, result.certificate)
        if result is checked TRUE:
            assert result.problem_key == ctx.problem_key
            witness = result.witness
            N = first_winning_turn(witness)  # 必ず以前のNより小さい。
            continue
        return UNKNOWN_WITH_CHECKED_TURN_INTERVAL
    return UNKNOWN_WITH_CHECKED_TURN_INTERVAL
```

手順取得は最短性の証明の信用基盤に含めず、その再実行だけを検査する。
`problem_key` は登録照合済みrule_idを含むProblemの全フィールドの比較用表現である。
再実行・被覆検査・最終判定には同じctx.bundleを渡し、途中で表示名から別の規則を引き直さない。
別実行の検査済みフラグは信用せず、同じ登録内容で再実行・再検査する。
証明済みの `d(h)=偽` と長さNの勝利手順があれば、途中でも `h+1<=最短ターン数<=N` を返せる。
`h=N-1` へ届いていなければ最短確定としない。

必要な `(turn,q)` は、補足仕様第14節のkernelが全出力先から作る支持集合で決める。
支持集合は `PK` bit/層で保存できる。具体的な個体の前向き探索を、その作成に持ち込まない。
補完が要求する行き先も含め、未登録を理由に必要な根を省略しない。

**14. 証明書の独立検査**

否定証明には以下の1〜5、最短性の確定には6も必要である。

1. rule_idの不変な登録内容と、seed・生のS0・開始ターン・観測・行動制約を照合する。数値設定やprofileの上書きを認めず、証明のHも確認する。
2. kernelがS0の外包包含とセル分割を検査し、提出されたpartitionで支持集合を初期セルから再構築して、全必要根の合法遷移を検査する。
3. 全詳細葉・補完の継続出力が次の外包に入り、終端を含めて資源が非負である。出力を切り落としていない。
4. `B0(live)=-infinity`、全hで `B[h][G]=0` を確認し、各層で必要な全辺について `B[h][q] >= Wtau+B[h-1][q']` が成立する。
5. 初期状態で `Q*E0 > B[H][q0]+u*A0+v*M0+w*I0`、または抽象成功経路が存在しない。
6. `H=N-1` とし、同じ登録規則・Problemで正確に再実行して初めてNターンで撃破する勝利手順がある。

項目2を、生成済み辺一覧への検査だけで済ませてはならない。
補足仕様の検査器が、根領域、各pcの命令、分岐の両側、乱数読み、更新式、出力包含を再計算する。
検査済みの葉と補完から辺を導出して、出力包含と項目4の不等式へ進む。
これによって、生成器がそもそも提出しなかった非空の枝も検出できる。
検査対象の一ターンの規則と現C++・利用者の指定規則との対応は、別途固定・監査する必要がある。

補完辺についても、その更新量の上限と行き先集合の被覆を確認する。
符号付き64bit整数の `-infinity` は専用タグにし、最小整数へ普通の加算をして表現しない。
オーバーフロー、未被覆領域、未処理の乱数位置があれば、証明は不成立。

乱数テープの端で探索を切って偽としてはならない。
使用する位置範囲 `P` が全対象経路を含むことを、1ターンの最大消費数とHから検証するか、
範囲を超える可能性を「上限不明」として伝播させる。
既存テープが5000要素であることは、5000以降へ行く経路がないことの証明ではない。

この検査は、到達可能な全個体を再探索するものではない。
一ターンの局所更新式への不等式を、有限個のガード領域ごとに検査する。
それらをHターン接続した場合の正しさは、第8節の相殺の証明が担う。

**15. メモリと数百ms〜15秒の扱い**

主要な保存対象は次のとおり。

| 対象 | 大きさ |
| --- | --- |
| 乱数テープ | `O(P)` |
| 述語セル | `O(PK)` |
| 局所更新と補完の項 | `O(8PK(J+C))` |
| ガード証明記録と検査作業領域 | 総byte数を別に制限。辺数から容量を決めつけない |
| DPの2層 | `2×8PK` byte |
| 全層の証明値を残す場合 | `(H+1)×8PK` byte |
| 実状態 | 必要な入力・再実行用だけ。各 `uint64_t[3]` |

`hotel[position]` は直接添字、セルは葉番号、辺は連続配列でよい。
大量の実状態の照合表、親付きの個体履歴、無制限のPareto集合は不要。
同じ完全な辺の記述を統合するなら、整列して完全比較できる。

数百ms〜15秒を論じる際は、以下を同じ計時に含める。

```text
入力検査 + 勝利手順取得・再実行 + 乱数準備 + 初回の局所記号実行 + 必要部分の再開
+ 領域制限・接続更新・支持集合・補完用の区間最大表 + 価格DP + 証明ノードと不等式の検査
```

巨大な前計算を計時外へ逃がして「5秒で証明」としてはならない。
既に利用可能な同一問題用の証明表がある場合だけ、再利用分と初回費用を別に明示する。

数値の目安を一つ挙げると、到達セル12,000、保存辺150,000、H=20、価格候補8回なら、
DP本体は最大約2,400万回の辺評価である。
全層の証明値は約2MB、辺を32byte/本で保存できれば約4.8MB。
これは演算量の例であり、この戦闘がそのサイズへ収まるという測定結果ではない。
この容量例にはガード証明記録と検査作業領域は含まれていない。
DPよりも、局所被覆生成・検査と、補完やセルの曖昧さを減らす反復の費用を先に評価する。

実装時には各工程の時間、実際のセル数、詳細辺数、補完辺数、証明の余裕
`Q*E0-bound` を出す。
加えて、証明ノード処理数、局所実行の再開回数、再利用した葉の割合、
最大経路上の補完数、追加述語数を計上する。
時間切れはUNKNOWN。検査した否定証明と、隣接するターン数の勝利手順がそろった場合を最短性の成功とする。
300ms・2秒・15秒の累積処理量・容量・価格候補数の開始設定は、補足仕様の第16節に従う。

**16. 提示された実戦ログでの検証条件**

利用者が「確殺＝解なし」と明示した例は、次の2件。

```text
0 3 2 11 a15 10 a16 14 10 11 12
0 3 5 15 11 11 12 10 17
```

これらを負例として使う。ただし、現エミュレーターで入力・seed・局面を再確認してから使う。
履歴から当時の探索アルゴリズムを取得する必要はない。

「麻痺」は解なしとは限らない。「普通」は最短ターン数が分かっているという意味ではない。
「ずれた」やパーサーの疑いの記録を、都合のよい正解ラベルに変換しない。
同一入力の繰り返し測定は、異なる独立問題として水増ししない。

必須の正例は、正本で再実行できる既知の勝利手順である。
その長さがLなら、少なくとも `d(L)=真` と矛盾する偽の証明書を出さないことを検査する。
`d(L-1)` は本当に未解決の対象として扱い、あらかじめ偽と決めつけない。

現ソースの初期値は定義から読み出す。
特に `setting::herbcount=8` と、現在の `BasePlayers[0].medicinal_herbs_count=7` は別である。
この証明の `I0` にどちらを入れるかを推測で決めず、実際の開始状態の値を使う。

局所遷移の検証では、HP228・114の直前直後、怒り中と非怒り中、
アクロの回避・カウンター・失効、眠り・麻痺の継続と解除を区別する。
FLEEについては、行動可能時の引っ越しが残ること、眠り・麻痺中には選べないこと、
選択後に敵が先に行動不能にした場合に逃走の効果が出ないことを別々に確認する。
未対応の眠り処理や、現コードのFLEEによる状態異常処理の迂回を、正解データとして固定しない。

**17. 最初に実装して可否を判断する範囲**

実装担当者は、次の順に可否を判断する。

1. 一ターンの規則に沿う証明記録を生成・独立検査し、非空の枝・比較・乱数読みを欠落させた記録を拒否できるか確認する。
2. セル分割後に既存の更新式を領域制限して再利用できるか確認し、小さい分割で価格0の上限を計算する。
3. 価格を最適化して、偽を示す余裕が出るか確認する。
4. 上限を決めている不一致だけを分割し、固定予算内で余裕が改善するか確認する。
5. 同じ入力の `d(N-1)=偽` と `d(N)=true` を組にして検査し、手順取得・準備込みの時間を測る。

最初の成功条件を「DPが速く動いた」にしない。
記号実行と欠落検出の検査が成立し、小さい分割で補完への依存が減ることを先に確認する。

価格0で上限が大きいことだけで失敗とはしない。
一方、価格と少数の分割でも上限が下がらず、補完やセルの曖昧さが支配的なままなら、
この証明の族は数百ms〜15秒の目的に不十分と判断する。
全状態展開へ戻したり、個体の追い出しを追加したりして成功に見せない。

今回必要なのは、この証明方式が対象局面で小さな証明書を作れるかの判定である。
汎用の記号戦闘エンジン、巨大な行列基盤、GPU移植を完成させることではない。

**実装担当者への到達目標**

正確な入力に対して、数百ms〜15秒を目標に第14節で検査できる否定証明と勝利手順の組を返すこと。
それができなければ、到達した上限と、価格・セル・局所被覆のどこで締まらなかったかを返す。
本書はそのための十分条件と有限予算の算法であり、対象局面での成功を先取りした証明書ではない。

**18. 前半系の横展開でも維持する証明形式**

指定されたbilyouma・zilyadamaの4ファイルから、効果の追加は有限mode、
MP吸収・MP回復・消耗品の追加は自座標の加減算とclampで表せることを確認した。
敵行動回数・予約された眠り行動・バフ減算の位置も、固定規則と有限な局所Frameで扱える。
各事実の範囲、指定外の未確認事項、実行時MP不足などの修正規則の必要箇所は補足仕様第17・18節に定める。
18ブランチすべての対応を確認したという意味ではなく、まずyo2_beのPoCを優先する。

採用する消耗品がk種類なら、資源を

\[
\mathbf r=(A,M,I_1,\ldots,I_k),\qquad
\Phi(S)=\lambda\cdot\mathbf r(S),\quad
\lambda\in\mathbb Z_{\ge0}^{k+2}
\]

に拡張する。撃破対象のEは引き続き別の座標とする。
各詳細葉で資源jの更新が `r'_j=r_j+c` または定数なら、端点での最大化も同じである。
Eは上端、その他の資源は下端を用い、

\[
W_\tau\ge Q(E-E')+\lambda\cdot(\mathbf r'-\mathbf r)
\]

を検査する。成功経路について足し合わせれば、同じ相殺から

\[
QE_0>B_{N-1}(q_0)+\lambda\cdot\mathbf r_0
\quad\Longrightarrow\quad d(N-1)=\mathrm{偽}
\]

が得られる。MPが途中で回復してもこの証明は成立する。
必要なのは、回復を辺の重みに含めること、全遷移の被覆、撃破時の資源と係数の非負である。
MP単調減少は、この定理自体の仮定ではない。
ただし、補充を許した外包で `M<=M0` を使うことはできない。

例えばMP回復量の上限をg、まほうのせいすいの価格を `w_water` とした場合、
残りの回復・消費部分のケースは `MP増分<=g, アイテム増分=-1` として、
対応する重み上限 `v*g-w_water` を一組で使う。
未実行ケースは別に覆い、MPだけ回復してアイテム消費を免れる通常葉を作らない。
薬草一種類の座標へ、複数種類の独立した所持数を黙ってまとめることもしない。

量子化する価格の分母Qは共通でよく、候補を非負整数へ丸めた後、必ず全辺の重みとDPを再計算する。
資源が増えれば価格候補の次元も増えるが、各候補の最適性は不要である。
一部の価格を0に固定する、複数の価格に同じ値を提案する等は、候補族を狭くする安全な選択である。
そのために合法行動や資源の実更新を消してはならない。
3価格・4変数LPというPoCのサイズを、消耗品追加後も無条件に使えるとはしない。
価格の全量子化格子の直積列挙も要求しない。

領域は、各資源の整数区間と、登録した有限modeのmaskの直積を維持する。
初期値・最大値・全更新から固定形式の外包をkernelが作り、入力と全出力の保存検査を行う。
バフ段階から実効能力値を導く対応、無効タイマーを省略する対応はRuleProgramの登録時に監査する。
「守備力が高いほど有利」「素早さが高いほど有利」という仮定で枝を落とさない。
能力値の違いが先攻・ダメージ・追加乱数を変えるため、必要な全modeを上限へ含める。

ブランチごとに登録する項目は、次に限定する。

| 登録項目 | 必要な内容 |
| --- | --- |
| 資源の型 | 敵・味方最大HP、MP上限の根拠、消耗品ごとの初期数。補充可能な軸を区別する |
| 効果の型 | 所有者、OFF/ONと段階・負の解除カウンタの有限値、初期条件と全更新 |
| 行動規則 | 合法な選択集合、実行時の状態・残量の再判定、行動枠と減算pc、固定ループ・再選択の上限 |
| 数値と乱数 | 対象ビルドの限定関数、引数と消費上限、カメラ・ローテーション、ターン番号の使用箇所 |
| 補完 | 許可pc、未実行の行動と資源収支、出力外包と全行き先 |

この登録は、生成器が自由に提供するInvariantコールバックではない。
登録した定数・型・固定プログラムから、補足仕様の小さい領域操作で検査できる形式に限る。
未登録の効果や実行時MP不足の処理があれば、その問題の証明を受理しない。
後半系をこの方式へ切り替える設定や実装は追加しない。

計算量も、実際に増える分を計上する。
合法コマンド数を `B_act`、ターンから決まる異なる実行条件の種類数をFとすると、
保存する詳細・補完項の総数Lには、保守的に

\[
L\le F\,B_{\rm act}PK(J+C)
\]

の上限を使える。yo2_beではF=1である。指定zilyadamaで確認した行動回数の奇偶は2クラスを要し、
プロファイル全体のターン依存性がこれだけだと確認できた場合にF=2とできる。
ローテーションやバフはセル内の制御座標で扱い、その直積をFへ追加列挙しない。
実行条件をこれだけに縮約してよいことも規則から確認する。
資源価格の次元をdとし、価格ごとに各辺の重みを一回計算してからDPを回せば、
重み計算とDPの合計は安全側に `O(R*L*(d+H))` と見積もれる。
Boxの座標操作、mode表の各入力値の処理、再開・検査、支持集合と区間最大表は別にVとbyte数へ数える。
局所証明の再利用が減る場合や必要なセル分割が増える場合も、PoCと同じ実時間予算で測る。

受理する結論は一貫して、同じ問題についての `d(N-1)=偽` と `d(N)=true` の組である。
型や資源軸を増やせることは、隣接するN-1の上限が十分締まることや、15秒以内の成功を保証しない。
最初の実測対象はyo2_beとし、指定の二例への横展開はその後の別の評価対象にする。
